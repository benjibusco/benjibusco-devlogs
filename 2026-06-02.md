# Dev Log — June 2, 2026

## Focus for Today

Completed the full staging API environment rollout for the lab authentication ecosystem and validated isolated production-style authentication infrastructure.

Focused on:

- staging API architecture
- PostgreSQL staging isolation
- PM2 multi-environment processes
- Apache reverse proxy configuration
- cross-origin authentication
- session persistence
- browser auth validation
- staging CI/CD deployment workflows
- GitHub Actions debugging
- deployment infrastructure hardening

---

## Staging API Environment Completion

Completed the dedicated staging API environment:

```text
https://api.dev.benjibusco.cloud
```

Final validated architecture:

```text
https://lab.dev.benjibusco.cloud
        ↓
Apache
        ↓
Frontend auth guard
        ↓
https://api.dev.benjibusco.cloud
        ↓
Apache reverse proxy
        ↓
PM2 (benjibusco-api-staging)
        ↓
Express API
        ↓
PostgreSQL (lab_auth_staging)
```

Successfully separated staging infrastructure from production.

Production remains:

```text
main
→ api.benjibusco.cloud
→ PM2: benjibusco-api
→ PostgreSQL: lab_auth_prod
```

Staging now uses:

```text
staging
→ api.dev.benjibusco.cloud
→ PM2: benjibusco-api-staging
→ PostgreSQL: lab_auth_staging
```

---

## PostgreSQL Staging Database Setup

Created dedicated staging database:

```text
lab_auth_staging
```

using existing PostgreSQL server infrastructure.

Configured staging `.env` environment variables:

```env
PORT=3001
NODE_ENV=staging
SESSION_SECRET=...
DB_HOST=...
DB_PORT=...
DB_NAME=lab_auth_staging
DB_USER=benjibusco_auth
DB_PASSWORD=...
```

Verified:

- PostgreSQL connectivity
- environment variable loading
- connection pool configuration
- staging database isolation

Validated connections using:

```bash
psql
```

and Node-based connection tests.

---

## PM2 Multi-Environment Architecture

Created dedicated PM2 staging process:

```text
benjibusco-api-staging
```

while preserving existing production process:

```text
benjibusco-api
```

Updated deployment architecture to support:

- isolated environment restarts
- independent staging deployment
- environment-specific PM2 reloads

Verified:

- PM2 process persistence
- PM2 save functionality
- independent process uptime
- isolated environment behavior

---

## Apache Reverse Proxy Configuration

Refactored staging Apache virtual host configuration to properly proxy requests to the Node staging API.

Implemented:

```apache
ProxyPreserveHost On
ProxyPass / http://localhost:3001/
ProxyPassReverse / http://localhost:3001/
```

Resolved earlier issue where Apache was serving static HTML instead of forwarding requests to Express.

Verified:

```text
https://api.dev.benjibusco.cloud/status
```

now correctly returns live API JSON responses.

Validated:

- HTTPS
- reverse proxy routing
- SSL behavior
- Express API forwarding
- Apache proxy modules

---

## Authentication & Session Validation

Validated complete browser authentication flow using staging infrastructure.

Tested:

```text
/auth/login
/auth/me
/auth/logout
```

Verified:

- login success
- session creation
- cookie persistence
- logout destruction
- protected route validation
- authenticated page refresh behavior
- redirect preservation
- session persistence across refreshes

Validated:

- Express sessions
- PostgreSQL-backed auth flow
- frontend auth guard behavior
- cross-origin credentials
- cookie handling

---

## CORS Configuration

Expanded API CORS configuration to support staging frontend origin:

```text
https://lab.dev.benjibusco.cloud
```

Final allowed origins include:

- production lab
- staging lab
- local development environments

Validated:

- preflight requests
- credentialed requests
- browser auth requests
- session cookies
- frontend API communication

---

## Frontend Auth Environment Integration

Updated staging frontend auth integration to use:

```text
https://api.dev.benjibusco.cloud
```

instead of production API endpoints.

Validated:

- frontend auth guards
- login redirects
- protected route handling
- browser session persistence
- authenticated page refreshes

Resolved issue where frontend remained stuck on:

```text
Checking access...
```

due to production API references inside staging frontend auth scripts.

---

## GitHub Actions Staging Deployment Workflow

Created dedicated staging deployment workflow:

```text
.github/workflows/deploy-api-staging.yml
```

Architecture now supports:

```text
push → main
↓
deploy production API

push → staging
↓
deploy staging API
```

Deployment workflow now includes:

- `npm install`
- PM2 restart
- environment reloads
- PM2 save persistence

Implemented:

```bash
pm2 restart benjibusco-api-staging --update-env
pm2 save
```

and equivalent production deployment improvements.

---

## GitHub Actions Deployment Debugging

Diagnosed and resolved multiple GitHub Actions deployment failures.

Debugged:

- SSH authentication failures
- deployment timeouts
- workflow inconsistencies
- staging workflow instability
- deploy key confusion
- repository secret validation

Validated:

- VPS SSH configuration
- UFW rules
- Fail2Ban state
- SSH daemon port bindings
- GitHub Actions secret configuration
- deployment key consistency

Ultimately resolved staging deployment instability by rebuilding the staging workflow file and validating repeated successful deployments.

---

## SSH Deployment Key Cleanup

Reviewed and cleaned up GitHub Actions deployment key naming.

Standardized deployment key labeling to better reflect:

- shared VPS deployment usage
- multi-repository deployment workflows
- future infrastructure scaling

Reviewed:

- deploy key comments
- authorized_keys consistency
- GitHub Actions deploy identities

---

## Infrastructure Validation

Fully validated:

- isolated staging auth testing
- production/staging separation
- environment-specific deployments
- branch-based deployment workflows
- PM2 multi-environment architecture
- PostgreSQL multi-database architecture
- reverse proxy API architecture
- frontend/backend auth integration
- browser session persistence

Current staging environment is now production-style and safe for continued authentication development/testing without risking production systems.

---

## Current Infrastructure State

### Production

```text
main
↓
api.benjibusco.cloud
↓
PM2: benjibusco-api
↓
PostgreSQL: lab_auth_prod
```

### Staging

```text
staging
↓
api.dev.benjibusco.cloud
↓
PM2: benjibusco-api-staging
↓
PostgreSQL: lab_auth_staging
```

---

## Follow-Up Tasks

Remaining future work:

```text
- remove temporary deployment debug logging
- document staging deployment workflow
- verify reboot persistence behavior
- continue auth security hardening
- implement browser-close logout handling
- begin production auth rollout planning
```
