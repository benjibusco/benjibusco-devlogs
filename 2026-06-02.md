# Dev Log — June 2, 2026

### Focus for Today

Completed the full staging authentication infrastructure rollout for the lab ecosystem, including isolated PostgreSQL environments, PM2 multi-environment orchestration, Apache reverse proxy integration, frontend authentication validation, and branch-based staging deployment workflows.

## Authentication Infrastructure

### Staging API Environment

Completed the dedicated staging API environment:

```text
https://api.dev.benjibusco.cloud
```

Final validated architecture:

```text
https://lab.dev.benjibusco.cloud
        ↓
Apache
        ↓
Frontend auth guard
        ↓
https://api.dev.benjibusco.cloud
        ↓
Apache reverse proxy
        ↓
PM2 (benjibusco-api-staging)
        ↓
Express API
        ↓
PostgreSQL (lab_auth_staging)
```

Successfully isolated staging infrastructure from production systems.

Production environment:

```text
main
→ api.benjibusco.cloud
→ PM2: benjibusco-api
→ PostgreSQL: lab_auth_prod
```

Staging environment:

```text
staging
→ api.dev.benjibusco.cloud
→ PM2: benjibusco-api-staging
→ PostgreSQL: lab_auth_staging
```

### Authentication Flow Validation

Validated complete browser authentication behavior using staging infrastructure.

Tested:

```text
/auth/login
/auth/me
/auth/logout
```

Verified:

- session creation
- PostgreSQL-backed persistence
- cookie handling
- logout destruction
- protected route validation
- authenticated refresh behavior
- redirect preservation
- cross-origin credential handling

Validated full frontend/backend authentication integration across the staging environment.

### Frontend Auth Integration

Updated staging frontend authentication integration to use:

```text
https://api.dev.benjibusco.cloud
```

instead of production API endpoints.

Validated:

- auth guards
- login redirects
- protected route handling
- browser session persistence
- authenticated refreshes

Resolved issue where staging frontend authentication remained stuck on:

```text
Checking access...
```

due to production API references inside staging auth scripts.

## Database Architecture

### PostgreSQL Staging Isolation

Created dedicated staging database:

```text
lab_auth_staging
```

Configured isolated staging environment variables:

```env
PORT=3001
NODE_ENV=staging
SESSION_SECRET=...
DB_HOST=...
DB_PORT=...
DB_NAME=lab_auth_staging
DB_USER=benjibusco_auth
DB_PASSWORD=...
```

Validated:

- PostgreSQL connectivity
- environment variable loading
- connection pooling
- staging database isolation

Verified connections using:

```bash
psql
```

and Node-based connection tests.

## VPS Infrastructure

### PM2 Multi-Environment Processes

Created dedicated staging PM2 process:

```text
benjibusco-api-staging
```

while preserving the existing production process:

```text
benjibusco-api
```

Expanded the deployment architecture to support:

- isolated environment restarts
- environment-specific deployments
- independent PM2 reloads
- persistent multi-environment process management

Validated:

- PM2 persistence
- `pm2 save`
- isolated uptime tracking
- independent process behavior

### Apache Reverse Proxy Integration

Refactored Apache staging virtual host configuration to properly proxy requests to the staging Node API.

Implemented:

```apache
ProxyPreserveHost On
ProxyPass / http://localhost:3001/
ProxyPassReverse / http://localhost:3001/
```

Resolved earlier issue where Apache was serving static HTML instead of forwarding requests to Express.

Validated:

```text
https://api.dev.benjibusco.cloud/status
```

now returns live API JSON responses.

Verified:

- HTTPS handling
- reverse proxy routing
- SSL behavior
- Express forwarding
- Apache proxy modules

### Cross-Origin Configuration

Expanded API CORS configuration to support:

```text
https://lab.dev.benjibusco.cloud
```

Final allowed origins include:

- production lab
- staging lab
- local development environments

Validated:

- preflight requests
- credentialed requests
- session cookies
- frontend API communication

## Deployment Infrastructure

### GitHub Actions Staging Deployment Workflow

Created dedicated staging deployment workflow:

```text
.github/workflows/deploy-api-staging.yml
```

Deployment architecture now supports:

```text
push → main
↓
deploy production API

push → staging
↓
deploy staging API
```

Deployment workflows now include:

- `npm install`
- PM2 restarts
- environment reloads
- PM2 persistence

Implemented:

```bash
pm2 restart benjibusco-api-staging --update-env
pm2 save
```

and equivalent production deployment improvements.

### GitHub Actions Deployment Debugging

Diagnosed and resolved multiple GitHub Actions deployment failures.

Debugged:

- SSH authentication failures
- deployment timeouts
- workflow inconsistencies
- staging deployment instability
- deploy key confusion
- repository secret validation

Validated:

- VPS SSH configuration
- UFW rules
- Fail2Ban state
- SSH daemon bindings
- GitHub Actions secret configuration
- deployment key consistency

Resolved staging deployment instability by rebuilding the workflow configuration and validating repeated successful deployments.

### SSH Deployment Key Standardization

Reviewed and cleaned up GitHub Actions deployment key naming.

Standardized deployment key labeling around:

- shared VPS deployment usage
- multi-repository deployment workflows
- scalable infrastructure naming

Reviewed:

- deploy key comments
- `authorized_keys` consistency
- GitHub Actions deploy identities

## Infrastructure Validation

Validated:

- isolated staging authentication testing
- production/staging separation
- environment-specific deployments
- branch-based deployment workflows
- PM2 multi-environment architecture
- PostgreSQL multi-database architecture
- reverse proxy API architecture
- frontend/backend auth integration
- browser session persistence

The staging environment is now production-style and safe for continued authentication development without risking production infrastructure.

## Future Plans

Remaining future work:

- remove temporary deployment debug logging
- document staging deployment workflows
- verify reboot persistence behavior
- continue authentication security hardening
- implement browser-close logout handling
- begin production authentication rollout planning
