# Dev Log — May 18, 2026

## Focus for Today

Continued strengthening foundational JavaScript and frontend engineering skills through iterative development of the sandbox `todo-list` experiment.

Focused heavily on:

- DOM manipulation
- event-driven UI updates
- frontend state management
- dynamic element generation
- scalable frontend architecture reasoning

---

## Todo List JavaScript Progress

Continued expanding the `todo-list` sandbox experiment inside:

```text
sandbox/experiments/todo-list/
```

Practiced:

- callback flow reasoning
- DOM hierarchy construction
- nested element generation
- conditional UI rendering
- frontend state transitions
- event-driven application flow

Strengthened foundational understanding of:

- parent/child DOM relationships
- DOM tree structure
- event listener architecture
- frontend rendering flow
- dynamic UI synchronization

---

## Dynamic Todo Structure

Refactored todo item generation into nested DOM structures using:

```js
document.createElement();
appendChild();
```

Expanded todo item architecture into:

```text
li
├── button
├── span
└── button
```

Practiced:

- nested DOM composition
- append hierarchy order
- structural UI organization
- reusable component thinking
- scalable frontend layout planning

Strengthened understanding of:

- DOM node relationships
- live DOM updates
- component assembly before DOM insertion
- dynamic parent/child element attachment

---

## Delete Button Logic

Implemented delete button event handling using:

```js
addEventListener("click", ...)
```

and:

```js
.remove()
```

Practiced:

- dynamically attached event listeners
- scoped DOM references
- element-specific event handling
- UI state cleanup after deletion

Implemented conditional empty-state restoration using:

```js
todoList.children.length;
```

Strengthened foundational understanding of:

- DOM collections
- child element counting
- conditional rendering logic
- frontend application state flow

---

## Completed Task State Handling

Implemented initial completed-task architecture using:

```js
classList.toggle("completed");
```

Added dedicated checkbox interaction handling using:

- separate event listeners
- reusable completed-state logic
- dynamic UI state toggling

Expanded todo item structure to support:

- checkbox interaction
- completed-state rendering
- future checkmark-based UI styling

Implemented conditional checkmark rendering using:

- `classList.contains()`
- dynamic `textContent` updates

Practiced:

- state-based UI rendering
- reusable completed-state architecture
- conditional visual updates
- parent-driven state styling

Strengthened foundational understanding of:

- state-driven UI behavior
- conditional frontend rendering
- reusable interaction patterns
- separation of state vs visual styling

---

## Frontend Architecture & Debugging Practice

Practiced reasoning through:

- event execution order
- DOM mutation flow
- validation timing
- conditional logic flow
- frontend state synchronization

Strengthened debugging workflow habits including:

- checking saved files
- verifying selector targets
- inspecting live DOM updates
- isolating CSS vs JavaScript issues
- tracing UI state transitions through DevTools

---

## Current Todo App Status

Current functionality now includes:

- semantic todo structure
- form submission handling
- input validation
- dynamic todo generation
- nested DOM creation
- delete button functionality
- completed-task toggling
- checkbox interaction handling
- hidden empty-state messaging
- conditional empty-state restoration
- reusable UI state classes

Next planned implementation steps:

- completed-state styling
- checkmark UI polish
- reusable component styling
- localStorage persistence
- filtering/search behavior later in development
