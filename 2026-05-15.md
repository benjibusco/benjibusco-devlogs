# May 15, 2026 - Test Deployment

## Devlog Automation + Submodule Deployment Testing

- Verified the `ben-dev-log` automation successfully regenerates `devlogs.json` when a new `2026-*.md` file is added.
- Added `2026-05-15.md` as a test devlog and confirmed it appeared in the generated JSON index.
- Confirmed the lab repo submodule workflow updates `content/devlogs` after the devlog repo changes.
- Pulled the latest lab changes on the VPS and confirmed the new devlog appeared on `/docs/devlogs/`.
- Fixed the docs route issue by confirming the deployed path needed `docs/devlogs/index.html` present on the VPS.
- Cleaned up an empty untracked `styles/dashboard.css` file on the VPS so the server copy matched `origin/main`.
- Updated the lab deploy workflow so deployment now also runs:

```bash
git submodule update --init --recursive
```

- Confirmed this command will update all current and future submodules, not just `content/devlogs`.

### Notes

The devlog pipeline now follows this flow:

```text
Devlog repo push
  → devlogs.json auto-generates
  → lab submodule pointer updates
  → lab deployment pulls latest code
  → VPS updates submodules
  → docs page updates
```

Next step: test the updated deploy workflow by pushing this devlog update.
