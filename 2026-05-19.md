# Dev Log — May 19, 2026

### Focus

Continue expanding the `todo-list` sandbox project with frontend architecture review, JavaScript reasoning practice, responsive layout systems, reusable component styling, and UI polish.

Focused on:

- DOM architecture reasoning
- event-driven UI logic
- frontend state flow
- responsive layout structure
- flexbox behavior
- spacing systems
- reusable component styling
- interaction feedback
- state-driven styling
- component/page architecture separation

## JavaScript Fundamentals

### JavaScript Concept Reinforcement

Reviewed and practiced:

- `preventDefault()`
- `.trim()`
- truthy/falsy validation
- early return patterns
- `document.createElement()`
- `.appendChild()`
- `.remove()`
- `.children.length`
- `.classList.add()`
- `.classList.remove()`
- `.classList.toggle()`
- `.classList.contains()`
- `.textContent`
- conditional rendering logic

Strengthened understanding of:

- DOM hierarchy relationships
- parent/child structure
- event listener flow
- callback execution
- UI state synchronization
- state-driven frontend behavior

## Frontend Architecture

### Frontend Architecture Review

Reviewed:

- storing completed state on parent `<li>`
- separating JavaScript state from CSS presentation
- reusable UI state classes
- parent-driven styling systems

Practiced:

```js id="q5k9r2"
classList.toggle("completed");
```

as the state layer while CSS handled:

- line-through styling
- opacity changes
- visual presentation

### Dynamic DOM Generation

Reviewed:

```js id="m8v1t6"
document.createElement();
appendChild();
```

Strengthened understanding of:

- DOM insertion hierarchy
- nested component structure
- scalable UI composition

### React & Frontend Framework Concepts

Reviewed differences between:

- vanilla JavaScript DOM manipulation
- declarative frontend frameworks like React

Strengthened understanding of:

- manual DOM mutation
- state-driven rendering
- declarative UI architecture
- conditional rendering concepts

Reviewed how React focuses on:

- describing UI state
- rendering from state conditions
- automatic DOM updates

instead of manually manipulating DOM nodes directly.

### Frontend Architecture Reinforcement

Continued reinforcing separation between:

- JavaScript state logic
- CSS presentation
- layout systems
- interaction styling
- page-shell architecture
- component-level styling

Practiced:

- reusable UI architecture
- scalable styling patterns
- modular frontend structure
- shared design systems

## Sandbox Experiments

### Todo App UI System

Implemented:

- layered dark gradient background
- glassmorphism-style app card
- responsive centered layout
- reusable spacing structure
- rounded component styling
- shadows/transparency
- responsive width constraints
- mobile-first layout structure

Practiced:

- `min-height: 100vh`
- layered gradients
- `rgba()`
- `padding` vs `margin`
- container spacing ownership

### Responsive Layout Practice

Implemented:

- tablet and desktop breakpoints
- responsive spacing
- responsive typography
- fluid widths
- constrained layouts

Practiced:

- `@media (min-width: 768px)`
- `@media (min-width: 1024px)`
- progressive enhancement
- mobile-first architecture

### Flexbox & Layout Systems

Implemented:

- horizontal form layout
- centralized spacing systems
- responsive task layout
- aligned action placement

Practiced:

- `display: flex`
- `align-items`
- `gap`
- `margin-left: auto`
- `flex-shrink`
- `flex-grow`
- `min-width: 0`

Strengthened understanding of:

- parent-controlled spacing
- flex container behavior
- responsive form layouts
- action alignment patterns

### Component Styling

Implemented:

- custom delete button styling
- hover interactions
- transition animations
- custom checkbox styling
- completed-task styling
- line-through states
- opacity-based completion states
- custom focus styling
- accessible focus visibility

Practiced:

- state-driven CSS styling
- hover feedback systems
- reusable component architecture
- interaction polish
- replacing browser defaults intentionally

## Sandbox Architecture

### Sandbox Integration & Shared Styling

Implemented:

- sandbox hero integration for experiment pages
- shared sandbox page structure
- experiment navigation hierarchy
- reusable experiment page layouts
- scoped todo component styling
- separation between page-shell styles and app styles

Refactored from:

```text id="v4p2n7"
todo.css controlling the full page
```

to:

```text id="t7m8q5"
sandbox.css controlling the page shell
todo.css controlling the todo component
```

Strengthened understanding of:

- scoped component styling
- shared layout systems
- reusable page architecture
- stylesheet layering
- component-based frontend organization
- global vs component-level selectors

### Sandbox Experiments Section

Implemented a dedicated `/sandbox/experiments/` landing page with:

- reusable sandbox hero section
- experiment card grid
- responsive project cards
- project descriptions
- hierarchical navigation structure

Added experiment cards for:

- Todo App
- Modal Playground
- Fetch Weather

Strengthened understanding of:

- scalable project organization
- reusable navigation systems
- frontend ecosystem hierarchy
- dashboard-style architecture

## UI Refinement

### Interface Refinement

Refined:

- task item borders
- spacing consistency
- component contrast balance
- visual hierarchy
- whitespace distribution
- checkbox emphasis
- delete action visibility

Practiced:

- visual noise reduction
- layered dashboard styling
- spacing-driven UI structure

## Future Plans

### Next Milestone

- localStorage persistence

### Planned Focus

- persistent frontend state
- saving/restoring todos
- completed-state persistence
- array/object-based todo state
- render-function architecture
- state-driven rendering
- React preparation
