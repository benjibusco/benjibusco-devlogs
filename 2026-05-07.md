# Devlog — May 8, 2026

## Infrastructure / VPS

- Claimed and configured `benjibusco.cloud` domain through Hostinger
- Configured `dev.benjibusco.cloud` as a staging environment subdomain
- Pointed DNS records to Linux VPS
- Debugged DNS propagation and incorrect A record IP issues
- Verified Apache virtual host configuration for staging environment
- Configured SSL certificate for `dev.benjibusco.cloud`
- Successfully tested staging environment setup from iPad using SSH and terminal

---

# BenKelly.Design — Staging Workflow

## Git / GitHub

- Created `staging` branch for `benkelly`
- Learned Git branch workflow using terminal commands
- Installed and explored Git Graph extension in VS Code
- Practiced Git staging, committing, merging, and branch visualization workflows

## GitHub Actions / CI-CD

- Added `deploy-staging.yml` GitHub Actions workflow
- Configured automatic VPS deployment from `staging` branch
- Successfully deployed staging branch automatically through GitHub Actions
- Verified successful GitHub Actions deployment pipeline using green checkmark workflow

## Staging Environment

- Cloned `staging` branch directly onto VPS staging environment
- Configured staging site at:

```text
https://dev.benjibusco.cloud/benkelly
```

## Asset Path Refactor

- Refactored HTML image paths from root-relative to portable relative paths
- Refactored CSS image paths to use proper relative file-based paths
- Verified compatibility across:
  - production
  - staging
  - GitHub Pages

## Git Workflow Improvements

- Added `.gitignore` file to repo
- Added local-only `git-workflow.md` notes file
- Configured `.gitignore` to exclude personal workflow notes from Git tracking

---

# KralBenji — Staging Workflow

## Git / GitHub

- Created `staging` branch for `kralbenji`
- Configured GitHub Actions staging deployment workflow
- Connected `staging` branch to VPS deployment pipeline

## Staging Environment

- Created VPS staging directory for `kralbenji`
- Cloned `staging` branch onto VPS staging environment
- Configured staging site at:

https://dev.benjibusco.cloud/kralbenji

## Navigation / Asset Refactor

- Refactored navigation links for staging compatibility
- Updated navigation structure to use relative links instead of root-relative links
- Refactored image paths inside `/pages/` directory for staging compatibility
- Updated favicon paths for portable deployment compatibility

## Apache / Debugging

- Diagnosed `.htaccess` clean URL rewrite conflicts with subdirectory staging environment
- Temporarily disabled `.htaccess` on staging for troubleshooting
- Debugged Apache virtual hosts and HTTPS routing behavior
- Verified direct file serving through Apache and curl testing
- Identified browser caching / redirect caching issues during staging tests

## Deployment Workflow

- Successfully merged `staging` branch into `main`
- Triggered production deployment pipeline through GitHub Actions
- Verified production deployment workflow for `kralbenji.com`

---

# Knowledge / Workflow Growth

- Learned practical CI/CD deployment workflow concepts
- Learned YAML-based GitHub Actions configuration structure
- Learned staging → production merge workflow
- Learned portable asset path strategies for multi-environment deployment
- Learned branch-based deployment workflows using Git and GitHub Actions
- Learned how browser redirect caching can affect frontend debugging
