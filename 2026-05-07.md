# Dev Log — May 7, 2026

### Focus for Today

Set up the first staging environments, configure CI/CD deployment workflows, and establish multi-environment deployment architecture across the VPS ecosystem.

## VPS Infrastructure

### Domain + Staging Environment Setup

Claimed and configured `benjibusco.cloud`, created `dev.benjibusco.cloud` as a staging environment, pointed DNS records to the VPS, and debugged DNS propagation and A record issues.

Verified Apache virtual host configuration, configured SSL for `dev.benjibusco.cloud`, and successfully tested SSH/VPS access from iPad using Termius.

## Deployment Infrastructure

### BenKelly.Design Staging Workflow

Created a `staging` branch for `benkelly` and practiced Git workflows including staging, committing, merging, and branch visualization using Git Graph in VS Code.

Added `deploy-staging.yml` GitHub Actions workflow and configured automatic VPS deployment from the `staging` branch.

Configured the staging environment at:

```text id="rfrk4v"
https://dev.benjibusco.cloud/benkelly
```

Refactored asset paths for portability by converting image and navigation paths from root-relative to relative paths compatible with:

- production
- staging
- GitHub Pages

Added `.gitignore` rules and excluded local-only workflow notes from Git tracking.

### KralBenji Staging Workflow

Created a `staging` branch for `kralbenji`, configured GitHub Actions deployment workflows, and connected the branch to VPS deployment automation.

Configured the staging environment at:

```text id="12v5ul"
https://dev.benjibusco.cloud/kralbenji
```

Refactored navigation, image paths, and favicon paths for multi-environment compatibility.

Diagnosed `.htaccess` rewrite conflicts caused by subdirectory staging environments, temporarily disabled rewrite rules during testing, and debugged Apache routing and caching behavior using direct file serving and `curl`.

Merged `staging` into `main` and verified successful production deployment through GitHub Actions.

## CI/CD + Workflow Architecture

Strengthened understanding of:

- CI/CD deployment workflows
- YAML-based GitHub Actions configuration
- staging → production merge workflows
- branch-based deployments
- portable asset path strategies
- browser redirect caching during debugging
