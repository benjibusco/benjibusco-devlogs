# 📅 Devlog — May 11, 2026

## 🧠 Focus for Today

Began transitioning the lab monitoring architecture from mock JSON-based infrastructure rendering into a real backend API ecosystem powered by Node.js and Express.

---

## ✅ Tasks Completed

### 🌐 Node.js API Project Initialization

Created the foundational backend API project structure for the lab ecosystem.

Worked inside:

```text
/var/www/api.benjibusco.cloud
```

Established backend architecture folders including:

```text
app/
logs/
backups/
deploy/
```

Initialized the Node.js application using:

```bash
npm init -y
```

Installed Express:

```bash
npm install express
```

Created the first backend API server file:

```text
server.js
```

---

## 🔧 Express API Foundation

### Built First `/status` Endpoint

Created the first live Express route for infrastructure monitoring:

```js
app.get("/status");
```

Implemented structured JSON responses matching the frontend dashboard architecture.

API responses now include:

- overview service states
- infrastructure service checks
- resource metrics
- timestamp metadata

This established the backend contract that the frontend dashboard will consume moving forward.

---

## 🧱 Backend Architecture Planning

### Established Full-Stack Infrastructure Separation

Separated responsibilities between:

| Layer              | Responsibility        |
| ------------------ | --------------------- |
| Frontend Dashboard | Visual rendering      |
| Express API        | Infrastructure data   |
| PM2                | Process management    |
| Apache             | HTTPS routing / proxy |
| VPS                | System resources      |

This reinforced real-world full-stack deployment architecture concepts.

---

## 📁 Deployment & Git Workflow Refinements

### Improved Backend Repository Structure

Refined `.gitignore` strategy for backend infrastructure projects.

Kept ignored:

```gitignore
logs/
backups/
node_modules/
.env
```

Removed:

```gitignore
deploy/
```

Reasoning:

- deployment scripts should remain version controlled
- deployment workflows are part of project architecture
- infrastructure automation is valuable project documentation

This reinforced deployment-focused repository organization practices.

---

## 📊 Initial API Testing Workflow

### Verified Live Backend Responses

Successfully tested live Express responses using:

```bash
curl http://localhost:3000/status
```

Confirmed:

- Express routing functionality
- JSON response formatting
- backend service accessibility
- frontend-compatible response structures

This marked the first successful live backend infrastructure response inside the lab ecosystem.

---

## 🧠 Concepts Practiced / Reinforced

- Express server architecture
- Node.js application setup
- backend folder organization
- VPS deployment structure
- infrastructure project organization
- API route creation
- JSON response design
- deployment-oriented Git workflows
- frontend ↔ backend architectural separation

---

## 🚀 Result

Successfully established the foundational backend API layer for the Benjibusco lab ecosystem.

The project architecture now supports future integration of:

- PM2-managed services
- Apache reverse proxying
- live VPS monitoring
- HTTPS API routing
- real infrastructure telemetry
- backend-powered dashboard rendering

The lab ecosystem officially transitioned from:

```text
Frontend Prototype Architecture
```

toward:

```text
Full-Stack Infrastructure Platform
```
