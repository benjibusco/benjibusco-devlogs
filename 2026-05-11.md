# Dev Log — May 11, 2026

# Focus for Today

Begin transitioning the lab monitoring system from mock JSON rendering into a real backend API ecosystem powered by Node.js and Express.

---

# Node.js API Project Initialization

Created the foundational backend API project structure inside:

```text
/var/www/api.benjibusco.cloud
```

Established backend directories:

```text
app/
logs/
backups/
deploy/
```

Initialized the Node.js application using `npm init -y`, installed Express with `npm install express`, and created the first backend server file:

```text
server.js
```

---

# Express API Foundation

Built the first live infrastructure monitoring route using:

```js
app.get("/status");
```

Implemented structured JSON responses matching the frontend dashboard architecture.

API responses now include 'overview service states, infrastructure service checks, resource metrics, and timestamp metadata'.

This established the backend contract the frontend dashboard will consume moving forward.

---

# Backend Architecture Planning

Established full-stack infrastructure separation between:

| Layer              | Responsibility        |
| ------------------ | --------------------- |
| Frontend Dashboard | Visual rendering      |
| Express API        | Infrastructure data   |
| PM2                | Process management    |
| Apache             | HTTPS routing / proxy |
| VPS                | System resources      |

This clarified the separation between frontend rendering, backend orchestration, infrastructure services, and deployment systems.

---

# Deployment + Git Workflow Refinements

Refined `.gitignore` strategy for backend infrastructure projects.

Kept ignored:

```gitignore
logs/
backups/
node_modules/
.env
```

Removed:

```gitignore
deploy/
```

Deployment scripts remain version controlled because deployment workflows are part of the infrastructure architecture and documentation system.

---

# Initial API Testing Workflow

Verified live Express responses using:

```bash
curl http://localhost:3000/status
```

Confirmed 'Express routing, JSON response formatting, backend accessibility, and frontend-compatible response structures'.

This marked the first successful live backend infrastructure response inside the lab ecosystem.

---

# Result

Successfully established the foundational backend API layer for the lab ecosystem.

The architecture now supports future integration of:

- PM2-managed services
- Apache reverse proxying
- live VPS monitoring
- HTTPS API routing
- real infrastructure telemetry
- backend-powered dashboard rendering
