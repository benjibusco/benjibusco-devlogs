# Dev Log — May 14, 2026

### Focus for Today

- Build the first real version of the lab documentation/devlogs platform
- Integrate markdown rendering into the lab ecosystem
- Connect the devlog repository directly into the lab using Git submodules
- Begin evolving the lab into a larger engineering workspace platform

## Documentation Platform

### Docs Architecture

Implemented `/docs/devlogs/` as a dedicated documentation route inside the lab ecosystem.

The lab evolved from a simple status dashboard into a larger engineering workspace and internal documentation platform.

Created the initial docs architecture:

```text id="8m2z4v"
app/
├── docs/
│   └── devlogs/
├── content/
│   └── devlogs/
```

This separated documentation UI, markdown content, and rendering logic into a scalable documentation architecture.

Built a responsive docs layout with:

- hero section
- sidebar navigation
- markdown content panel
- sticky desktop sidebar

while keeping the styling visually consistent with the main dashboard.

Created `/styles/docs.css` with:

- markdown typography
- code styling
- blockquotes
- responsive docs layout

Adjusted typography sizing to feel more like documentation content instead of dashboard hero sections.

## Markdown Rendering System

### Dynamic Markdown Rendering

Integrated `marked.js` to render markdown files directly inside the lab interface.

Created `/scripts/docs.js` to:

- fetch markdown dynamically
- inject rendered HTML into the content panel
- handle loading errors gracefully

This transformed the docs system from static HTML into dynamically rendered engineering documentation.

### Sidebar Navigation + Metadata System

Built interactive sidebar navigation with:

- active entry highlighting
- click-based markdown loading
- dynamic content replacement

Created `/content/devlogs/devlogs.json` as a lightweight metadata/index system powering:

- sidebar generation
- devlog routing
- active entry management

Refactored the sidebar from hardcoded HTML into dynamically generated entries from JSON metadata.

Removed redundant hardcoded article titles and shifted responsibility for:

- document title
- heading hierarchy
- content structure

entirely into markdown files themselves.

The renderer now behaves more like GitHub documentation and markdown engineering wiki systems instead of a static dashboard card.

## Git + Repository Architecture

### Git Submodule Integration

Integrated the dedicated `ben-dev-log` repository directly into the lab ecosystem using `git submodule add`.

Mounted the repository into:

```text id="j5m7k2"
/content/devlogs/
```

This established:

- centralized markdown storage
- reusable documentation architecture
- independent Git history
- scalable documentation workflows

## Lab Ecosystem Architecture

### Engineering Workspace Structure

Further refined the architecture distinction between:

```text id="z1p8w4"
/dashboard/  -> observability
/docs/       -> engineering knowledge + devlogs
/tools/      -> operational utilities
/sandbox/    -> experiments + prototypes
```

Planned `/sandbox/` as a safe experimentation layer for:

- frontend testing
- React experiments
- API playgrounds
- deployment testing
- UI prototypes
- infrastructure experiments

## Deployment Infrastructure

### Devlog Automation System

Built an automated documentation pipeline using GitHub Actions.

Created `.github/workflows/update-devlog-index.yml` to:

- scan all `2026-*.md` files
- regenerate `devlogs.json`
- commit updated metadata automatically

Documentation flow:

```text id="n6v3d7"
Write markdown
      ↓
Push to GitHub
      ↓
GitHub Actions regenerates devlogs.json
      ↓
Lab platform renders updated content
```

Added cross-repository automation so `ben-dev-log` triggers the lab repository automatically using `repository_dispatch`.

Created a second workflow in the lab repository to:

- update the `content/devlogs` submodule
- commit updated submodule pointers
- trigger deployment automatically

Updated the deploy workflow to run:

```bash id="r4x2c9"
git submodule update --init --recursive
```

This allows all current and future submodules to deploy automatically without additional workflow changes.

Final deployment pipeline:

```text id="b7w5m1"
Devlog markdown update
        ↓
GitHub Actions regenerates devlogs.json
        ↓
ben-dev-log triggers lab repo
        ↓
lab updates submodule pointer
        ↓
lab deploy workflow runs
        ↓
VPS pulls latest repo + submodules
        ↓
docs page updates automatically
```

### Infrastructure + Deployment Refinements

Created a new dedicated GitHub Actions deploy SSH key after deployment authentication failures.

Configured:

- VPS deploy access
- GitHub Actions secrets
- recursive submodule deployment syncing

Resolved:

- detached HEAD confusion inside submodules
- submodule merge/update strategy issues
- VPS deployment synchronization problems
- recursive deployment workflow behavior

## Future Plans

Potential future improvements:

- markdown frontmatter support
- syntax highlighting
- searchable documentation
- docs categorization
- historical observability data
- deployment history tracking
- sandbox landing page
- engineering tools system
