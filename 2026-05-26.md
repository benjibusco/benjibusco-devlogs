# Dev Log — May 26, 2026

### Focus for Today

Refine shared frontend layout systems, expand reusable ecosystem scaffolding architecture, continue API platform planning, and begin building the first PostgreSQL-backed authentication prototype for future protected lab tooling.

Focused on:

- shared layout systems
- markdown spacing refinement
- reusable HTML templates
- sandbox architecture evolution
- API platform structure
- PostgreSQL architecture
- Express authentication systems
- session-based authentication
- relational database workflows

## Frontend Layout Architecture

### Global Layout + Hero Spacing Refinement

Refined global layout spacing across the lab ecosystem to better align with the tighter, more application-focused design direction established in sandbox projects and internal tooling pages.

Reduced excessive vertical spacing within the shared layout shell by updating tablet and desktop spacing:

```css id="x7m2q4"
.site-shell {
  padding: var(--space-xxs) var(--space-lg);
}
```

This tightened page entry spacing across:

- docs pages
- tools pages
- sandbox sections
- reusable templates

and pushed the ecosystem further toward a modern engineering dashboard and application-style aesthetic.

### Markdown Layout Spacing Cleanup

Refined markdown documentation page spacing by reducing unnecessary separation between hero sections and markdown layout containers.

Updated docs layout spacing:

```css id="r5v8n1"
.docs-layout {
  padding-top: var(--space-xxs);
}
```

This improved spacing consistency across:

- devlogs
- architecture pages
- workflow documentation
- future markdown-rendered sections

while preserving reusable token-driven spacing architecture.

## Reusable Template Architecture

### Reusable HTML Template System

Created reusable HTML template systems to standardize future ecosystem expansion and reduce repeated structural setup work.

Added reusable standard card layout template:

```text id="k3p9m7"
/templates/html/card-layout-template.html
```

Designed for:

- docs overview sections
- tools sections
- dashboard modules
- reusable card-grid layouts
- future ecosystem overview pages

Created reusable sandbox project template:

```text id="n6x1q5"
/templates/html/sandbox-project-template.html
```

Designed for:

- sandbox projects
- UI tests
- frontend experiments
- API tests
- isolated feature development

Created reusable markdown rendering template:

```text id="v8m4r2"
/templates/html/markdown-layout-template.html
```

Designed for:

- devlogs
- architecture pages
- workflow documentation
- markdown-rendered content systems

This established the beginning of a reusable internal scaffolding system for long-term ecosystem growth and architectural consistency.

## Sandbox Architecture

### Sandbox Architecture Refinement

Refined sandbox structure by evolving the previous “experiments” direction into a more structured “projects” architecture.

Updated sandbox direction toward:

```text id="p2q7v9"
sandbox/
├── projects/
├── ui-tests/
├── frontend-mentor/
├── freelance/
└── api-tests/
```

This better reflects the current purpose of the sandbox ecosystem, which now contains:

- structured engineering projects
- reusable frontend systems
- application architecture practice
- internal tooling prototypes
- isolated UI and interaction testing

instead of temporary experimentation.

Updated sandbox copy and card descriptions to reflect the project-oriented direction.

## API Platform Architecture

### API Section Architecture Planning

Planned the long-term frontend API architecture for the lab ecosystem.

Created a dedicated frontend API section:

```text id="m5t8x3"
/api/
```

Planned architectural separation between:

```text id="c7n1r4"
api.benjibusco.cloud
```

and:

```text id="q4v9m6"
lab.benjibusco.cloud/api/
```

Purpose separation:

```text id="w2p6k8"
/status/
→ ecosystem health + infrastructure monitoring

/api/
→ backend architecture + endpoint visibility + API documentation
```

Removed duplicate architectural directions including:

```text id="j9x3q1"
/api/status/
/api/tools/
```

to avoid overlapping with existing ecosystem sections.

Simplified the API architecture toward a single-page dashboard structure:

```text id="r6m2v7"
/api/
└── index.html
```

Planned dashboard sections for:

- API overview
- service architecture
- infrastructure metrics
- endpoint references
- deployment metadata
- backend architecture notes
- observability direction

### API Dashboard Initial Implementation

Built initial:

```text id="t1q8m5"
/api/index.html
```

using the reusable card layout template system.

Implemented:

- shared hero layout
- reusable card-grid structure
- API overview section
- service overview cards
- infrastructure metrics section
- endpoint reference section
- deployment information section
- backend architecture notes

Integrated references to:

```text id="z5v4p2"
https://api.benjibusco.cloud/status
```

Updated the main landing page ecosystem card from:

```text id="k8m6r1"
Backend API
```

to:

```text id="n3q7v8"
API
```

to better reflect the broader frontend and backend ecosystem direction.

Planned future live API integration using:

```text id="x1p5m9"
/scripts/api.js
```

for frontend metric rendering and observability systems.

## PostgreSQL Architecture

### PostgreSQL Local Development Setup

Began local PostgreSQL setup to prepare for future authentication systems and protected lab tooling.

Installed and configured:

- Homebrew
- PostgreSQL 16

Started PostgreSQL locally using:

```bash id="v4r2m7"
brew services start postgresql@16
```

Verified PostgreSQL service status through:

```bash id="c9q5n1"
brew services list
```

Created development authentication database:

```text id="m2v8p6"
lab_auth_dev
```

Created initial relational database tables:

```text id="p6x4r9"
users
projects
```

Implemented foundational relational architecture including:

- primary keys
- foreign key relationships
- timestamp defaults
- unique constraints
- relational joins

Created relational project ownership structure:

```text id="t7m1q3"
projects.user_id → users.id
```

Practiced:

- INSERT queries
- SELECT queries
- UPDATE queries
- WHERE filtering
- JOIN queries
- relational data flow

This established the first relational database architecture within the lab ecosystem.

## Authentication Backend Architecture

### Local Authentication Backend Prototype

Created a separate local backend authentication project:

```text id="w8q2m4"
lab-auth/
```

to safely prototype authentication systems outside the live VPS environment.

Initialized:

- Node.js
- Express
- PostgreSQL connection pooling
- environment variable configuration
- session infrastructure
- local backend routing architecture

Installed backend dependencies:

- express
- pg
- dotenv
- bcrypt
- express-session
- connect-pg-simple
- nodemon

Created backend structure:

```text id="r1v7m5"
lab-auth/
├── db/
├── routes/
├── server.js
└── .env
```

Configured PostgreSQL connection pooling using:

```js id="q9m3x8"
new Pool({
  connectionString: process.env.DATABASE_URL,
});
```

Implemented initial Express server architecture including:

- JSON middleware
- PostgreSQL-backed sessions
- environment variable loading
- session configuration
- route structure

Created initial health endpoint:

```text id="n5p2v6"
GET /health
```

Implemented live PostgreSQL API querying through:

```text id="k4x8m1"
GET /users
```

This established the first real:

- Express API route
- PostgreSQL query integration
- backend JSON response flow
- frontend and backend database architecture

Architecture flow:

```text id="m7q1r9"
Browser
↓
Express Route
↓
PostgreSQL Query
↓
JSON Response
```

## Authentication System Foundation

### Session-Based Authentication Foundation

Implemented the first authentication flow for the lab ecosystem.

Created:

```text id="x3v6p8"
POST /auth/register
```

with:

- bcrypt password hashing
- PostgreSQL user creation
- validation handling
- structured JSON responses

Implemented:

```text id="c5m2q7"
POST /auth/login
```

with:

- credential validation
- password hash comparison
- session creation
- authenticated user responses

Implemented protected authentication route:

```text id="j8r4n1"
GET /auth/me
```

using:

```js id="t6p9m3"
req.session.userId;
```

to validate authenticated sessions.

Configured PostgreSQL session storage using:

```text id="p1x5v2"
user_sessions
```

table architecture for persistent session handling.

Tested authentication flow using:

- curl requests
- cookie persistence
- protected route validation
- session-based authentication flow

This established the first complete local authentication system architecture for future protected lab tooling and internal admin systems.
