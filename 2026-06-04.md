# Dev Log — June 4, 2026

### Focus for Today

Transitioned the Benjibusco ecosystem away from GitHub Actions SSH deployments into a centralized VPS-side pull deployment framework with reusable deployment tooling, deployment observability, structured deployment metadata, API-powered monitoring, and live dashboard integration across production and staging environments.

## Infrastructure Identity + Repository Management

### GitHub Account Standardization

Renamed GitHub account from:

```text id="cnbl8q"
KralBenji
```

to:

```text id="x0k6nt"
benjibusco
```

to better align with infrastructure branding and deployment naming conventions.

Updated:

- VPS repository remotes
- deployment credentials
- deployment security configuration

Also:

- generated a new Bitwarden password
- increased credential strength
- reviewed deployment secrets

## Deployment Infrastructure

### VPS-Side Pull Deployment Architecture

Abandoned GitHub Actions SSH deployments after recurring GitHub runner connectivity failures.

Observed repeated:

```text id="dbmj90"
dial tcp
i/o timeout
```

failures despite:

- healthy VPS SSH access
- valid workflows
- correct firewall configuration
- operational SSH services

Replaced the deployment flow with:

```text id="b3hrj0"
Git Push
→ VPS Cron
→ Deployment Script
→ git fetch
→ Commit comparison
→ Deploy if changed
→ Update deployment JSON
```

Benefits:

- eliminated inbound deployment webhooks
- reduced GitHub Actions dependency
- centralized deployment execution
- simplified infrastructure scaling
- improved deployment observability

### Deployment Targets

Centralized deployment tooling inside:

```text id="74dbyu"
/var/www/api.benjibusco.cloud/app/scripts/deployments
```

Deployment targets:

#### Production

```text id="0q2izs"
benkelly-production
kralbenji-production
lab-production
api-production
```

#### Staging

```text id="kgp8wu"
benkelly-staging
kralbenji-staging
lab-staging
api-staging
```

Core deployment features:

- commit comparison
- automatic pull deployments
- lock file protection
- deployment JSON generation
- optional npm install
- optional PM2 restart
- optional submodule updates

Lab deployments automatically run:

```bash id="a0unl6"
git submodule update --init --recursive
```

API deployments automatically run:

```bash id="6g4ys0"
npm install --omit=dev
pm2 restart
```

### Cron Deployment Automation

Configured cron-based deployment polling across all environments.

Each deployment target now checks GitHub every minute and deploys automatically when changes are detected.

Validated:

- `git fetch` behavior
- commit comparison
- automatic deployments
- deployment logging
- deployment visibility

### Deployment Logging + Lock Protection

Created centralized deployment logs under:

```text id="j7c6v2"
/var/www/api.benjibusco.cloud/app/logs
```

Implemented deployment lock protection using:

```text id="mq2pj1"
/tmp/deploy-<target>.lock
```

Example:

```text id="h4kvra"
/tmp/deploy-lab-production.lock
```

Deployment flow:

```text id="v6ap6y"
deploy starts
→ lock created
→ deployment runs
→ lock removed
```

This prevents:

- overlapping deployments
- concurrent Git operations
- duplicate PM2 restarts
- deployment race conditions

Validated lock handling through manual testing.

## Deployment Observability

### Deployment Metadata System

Created centralized deployment tracking under:

```text id="n7d0mh"
/var/www/api.benjibusco.cloud/app/data/deployments
```

Standardized deployment metadata across all environments.

Deployment JSON structure now includes:

```json id="w24n1o"
{
  "target": "...",
  "project": "...",
  "environment": "...",
  "status": "...",
  "message": "...",
  "failed": false,
  "branch": "...",
  "commit": "...",
  "commitFull": "...",
  "commitDate": "...",
  "durationMs": 842,
  "durationSeconds": 0.84,
  "updatedAt": "...",
  "lastCheckedAt": "..."
}
```

Added support for:

- deployment duration tracking
- deployment failure tracking
- deployment timestamps
- deployment heartbeat monitoring
- structured deployment status messages

Deployment state now distinguishes between:

```json id="nq30tw"
updatedAt
lastCheckedAt
```

to separate:

- last deployment
- last deployment check

This prevented deployment timestamps from constantly resetting during cron polling.

### Deployment Failure Tracking

Implemented centralized deployment error handling using:

```bash id="gqjv7q"
trap handle_error ERR
```

Deployment failures now automatically:

- write failed deployment JSON
- update dashboard deployment state
- write failure logs
- preserve deployment metadata
- safely remove lock files

Validated failure handling using intentionally invalid deployment paths.

Verified:

- failed deployment JSON generation
- failed dashboard rendering
- deployment recovery
- automatic lock cleanup

### Deployment Verification Workflow

Established a repeatable deployment verification workflow:

```text id="9d4d0d"
run deployment
→ inspect JSON
→ inspect logs
→ inspect live dashboard
```

Verified all deployment targets successfully reported:

```text id="x4rmv4"
up-to-date
```

through:

- deployment logs
- deployment JSON
- dashboard deployment cards

## API Monitoring Architecture

### Deployment Monitoring API

Created deployment monitoring routes:

```text id="hpcntx"
routes/deploy.js
```

Added API endpoints:

```text id="rl6yo6"
GET /deployments
GET /deployments/:target
```

Deployment API now:

- reads deployment JSON files
- exposes centralized deployment state
- supports per-target deployment lookups
- powers dashboard deployment monitoring

### API Server Refactor

Temporarily simplified `server.js` while troubleshooting deployment behavior.

Later restored the richer `/status` response required by the Lab dashboard.

Re-added:

```js id="mshozx"
const os = require("os");
const { execSync } = require("child_process");
```

Restored monitoring support for:

- Apache status
- Node API status
- SSL status
- deployment status
- uptime
- memory usage
- disk usage
- CPU usage
- checked timestamps

## Frontend Monitoring Integration

### Deployment Dashboard Integration

Integrated deployment monitoring directly into the Lab dashboard.

Updated:

```text id="ymv7s4"
status/index.html
scripts/status.js
```

Dashboard now fetches:

```text id="g59c4o"
https://api.benjibusco.cloud/deployments
```

every 30 seconds.

Deployment cards now display:

- deployment status
- branch
- commit hash
- deployment duration
- deployment timestamps
- deployment heartbeat state

Implemented custom deployment ordering:

```text id="rurhho"
Lab Production
Lab Staging

API Production
API Staging

Ben Kelly Production
Ben Kelly Staging

Kral Benji Production
Kral Benji Staging
```

### Dashboard UI + Monitoring Refinements

Improved dashboard readability through:

- readable deployment naming
- improved grouping and spacing
- monospaced status styling
- deployment heartbeat styling
- failed deployment rendering

Added monitoring styling:

```css id="5g2t0f"
.deployment-check {
  opacity: 0.7;
  font-size: 0.9rem;
}
```

Initially tested relative timestamps:

```text id="jj05ka"
2m ago
```

Later switched to full timestamps:

```text id="tpkuc3"
6/4/26, 9:55 PM
```

while preserving:

```text id="e5w6ye"
Last Checked: just now
```

for operational heartbeat visibility.

### Live Deployment Validation

Validated the complete deployment monitoring pipeline:

```text id="fwlbof"
Git Push
→ VPS deployment
→ deployment JSON update
→ API deployment endpoint
→ dashboard deployment cards
→ matching commit hashes
```

Compared deployed VPS commits directly using:

```bash id="w71mfe"
git rev-parse --short HEAD
```

Confirmed dashboard deployment data accurately reflects live deployed environments.

## Reusable Deployment Framework

### Shared Deployment Library

Created centralized reusable deployment helper:

```text id="kgkt2y"
/var/www/api.benjibusco.cloud/app/scripts/deployments/deploy-lib.sh
```

Refactored all deployment targets to use the shared framework instead of duplicated per-project deployment logic.

### Deployment Wrapper Simplification

Deployment wrappers now only define:

```bash id="ehg1zk"
TARGET
PROJECT
ENVIRONMENT
SITE_PATH
BRANCH
```

Optional deployment behaviors:

```bash id="2m7uxn"
RUN_SUBMODULE_UPDATE="true"
RUN_NPM_INSTALL="true"
PM2_PROCESS="..."
```

Centralized:

- deployment execution
- lock handling
- deployment metadata writing
- failure tracking
- logging
- PM2 integration
- submodule handling

Future deployment improvements can now be implemented from a single shared deployment library instead of modifying every deployment target individually.

## VPS Infrastructure

### Backup Organization

Created deployment backup archive structure:

```text id="1vh4t6"
/var/www/api.benjibusco.cloud/backups/deployments-2026-06-04/
```

Improved separation between:

- live deployment systems
- operational backups
- rollback checkpoints

### Linux + Git Operational Workflow

Continued strengthening Linux and Git operational workflows.

Practiced:

- frequent `git status` usage
- repository boundary awareness
- relative path debugging
- shell redirection behavior
- deployment verification workflows

Discussed differences between:

```bash id="5snm58"
truncate -s 0 file.log
```

and:

```bash id="1t1p7n"
> file.log
```

including shell behavior, operational intent, and safety considerations.

### Frontend Asset + Path Debugging

Resolved a frontend deployment issue caused by editing:

```text id="d0d7q7"
status.js
```

instead of:

```text id="4qqeqg"
scripts/status.js
```

This reinforced understanding of:

- relative filesystem paths
- frontend asset loading
- browser-loaded script locations
- web server file structure

## Frontend Visual Refinement

### Typography + Background Atmosphere

Tested monospaced typography styling across the lab ecosystem on the staging branch to reinforce the infrastructure/dashboard aesthetic.

Refined shared background rendering inside `styles/base.css` by extending the existing radial glow further down the center of the page while preserving dark edge fade behavior and the existing color palette.

Implemented layered radial gradients to:

- preserve the existing hero glow
- avoid overly bright backgrounds
- maintain darker edge contrast
- improve long-scroll visual consistency

This improved observability/dashboard atmosphere while preserving readability and visual identity consistency.

## Architectural Milestone

Completed the transition from:

```text id="27m2be"
collection of deployment scripts
```

to:

```text id="l27b83"
centralized deployment framework
```

Current infrastructure now supports:

```text id="tql9cq"
Git Push
→ VPS Cron
→ Deployment Scripts
→ Deployment JSON
→ API Monitoring Routes
→ Live Dashboard Monitoring
→ Deployment Verification
```

This significantly improved:

- maintainability
- deployment consistency
- observability
- scalability
- operational tooling structure

## Future Plans

Planned future deployment improvements:

- deployment history tracking
- deployment duration analytics
- PM2 monitoring
- logs viewer
- clickable commit hashes
- environment filtering
- cron heartbeat monitoring
- stale deployment detection
- deployment notifications
- deployment health monitoring
- infrastructure tooling expansion

### Internal Documentation Planning

Planned private/internal documentation structure:

```text id="0ps4is"
docs/plans/
```

Architecture direction:

```text id="m7o0a4"
Public Docs
- architecture
- deployment
- devlogs

Private/Internal Docs
- plans
- infrastructure roadmaps
- deployment strategies
- internal tooling
```
