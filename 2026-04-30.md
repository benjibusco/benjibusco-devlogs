# Dev Log — April 30, 2026

### Focus for Today

Improve professional visibility, continue strengthening frontend development workflows, and harden VPS infrastructure/security.

## Career + Professional Presence

Updated the Handshake profile while refining:

- frontend development skills
- services/offerings
- project descriptions
- personal branding

## Frontend Development

### Frontend Mentor — FAQ Accordion

Completed a responsive FAQ accordion project using HTML, CSS, and JavaScript.

Built a reusable component structure, implemented accordion state management with JavaScript, enforced single-item expansion logic, and added state-driven icon swapping.

Refined responsive background behavior across breakpoints, pushed the project to GitHub, deployed using GitHub Pages, and submitted the completed assignment for grading.

### KralBenji.com Updates

Updated homepage button `href` paths for clean URL compatibility.

Implemented a mobile navigation refactor featuring:

- animated hamburger → X transitions
- transform-based slide-in navigation
- JavaScript-driven `aria-expanded` state handling
- responsive navigation resets across breakpoints
- improved z-index layering and interaction reliability

Replaced `nav-toggle` with a reusable hamburger component and standardized `site-nav` usage across HTML, CSS, and JavaScript.

Removed conflicting navigation styles from `hero.css`.

Fixed favicon `href` paths to correctly load from the site root across pages and browsers.

## VPS Infrastructure + Security

### VPS Security Hardening

Updated and rebooted the VPS to address security vulnerabilities.

Configured UFW firewall rules for SSH and Apache, cleaned duplicate entries, and installed/configured Fail2Ban for SSH brute-force protection.

Enabled the `sshd` jail with retry limits and ban timing.

Audited system users, confirmed no unauthorized accounts existed, locked the default `ubuntu` user, verified the root account remained disabled, and confirmed `PermitRootLogin` was disabled.

Reviewed login history and verified expected access sources including GitHub Actions and personal IP access.

## Education + Labs

### uCertify Labs — Chapter 7

Completed:

- Lab 7-1
- Lab 7-2
- Lab 7-3

Refactored labs into separate HTML, CSS, and JavaScript files while improving UI styling and component structure.

### GitHub Pages Deployment — Lab 7

Structured multiple projects inside a single repository using folder-based routing and `index.html`-driven clean URL navigation.

Created a landing page linking all lab projects, deployed using GitHub Pages, and verified routing functionality across all deployed labs.
