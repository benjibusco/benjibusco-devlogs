# Dev Log — May 16, 2026

## Focus for Today

Refined the markdown documentation system, strengthened Git submodule workflow understanding, expanded deployment/documentation architecture, and continued foundational JavaScript practice through a new todo application experiment.

---

## Markdown Documentation Refinement

Refined the markdown typography and spacing system inside `docs.css`.

Adjusted:

- heading scale
- heading spacing
- paragraph readability
- line-height
- list spacing
- code block sizing

Reduced oversized documentation hierarchy by tightening heading sizing/margins and removing excessive spacing from:

```css
gap: var(--space-md);
```

inside `.markdown-panel`.

Shifted spacing responsibility back to individual markdown elements for denser engineering-style documentation rendering.

---

## Git Submodule Workflow Reinforcement

Worked through additional Git submodule synchronization behavior involving `content/devlogs`.

Pulled upstream devlog updates using:

`git submodule update --remote content/devlogs`

Strengthened understanding of:

- submodule commit pointers
- detached submodule states
- parent repository synchronization
- local submodule tracking

Verified submodule updates do not overwrite unrelated local repository changes.

Created internal workflow documentation for:

- updating submodules safely
- protecting uncommitted lab changes
- syncing `ben-dev-log` with the `lab` repo
- separating submodule commits from feature commits

---

## Deployment Documentation Expansion

Created internal deployment documentation covering:

- deployment workflows
- GitHub Actions architecture
- rollback procedures
- deployment verification
- submodule deployment behavior
- VPS deployment flow

Added:

- `deployment-workflow.md`
- `github-actions.md`
- `rollback-procedures.md`
- `git-submodule-workflow.md`

Documented:

- GitHub Actions deployment responsibilities
- SSH deploy key workflows
- recursive submodule deployment behavior
- rollback recovery procedures
- deployment troubleshooting patterns

---

## Documentation + Sandbox Architecture

Expanded internal documentation architecture:

```text
docs/
├── architecture/
├── deployment/
├── devlogs/
└── infrastructure/
```

Added:

- `lab-architecture-roadmap.md`
- `lab-project-instructions.md`

Refined sandbox workspace planning for:

```text
sandbox/
├── experiments/
├── frontend-mentor/
├── freelance/
├── ui-tests/
└── api-tests/
```

Continued reinforcing:

- mobile-first frontend architecture
- reusable component systems
- modular CSS organization
- scalable project structure
- centralized engineering documentation

---

## Todo Application Experiment

Created a new isolated sandbox experiment:

```text
sandbox/experiments/todo-list/
```

Structured using:

```text
todo-list/
├── index.html
├── styles/
│   └── todo.css
└── scripts/
    └── todo.js
```

Built and refined the initial semantic HTML structure using:

- `<main>`
- `<section>`
- `<form>`
- `<label>`
- `<ul>`

Practiced:

- accessibility-focused form structure
- reusable class naming
- scalable frontend organization
- BEM-style naming patterns

---

## JavaScript Fundamentals Practice

Started foundational JavaScript implementation for the todo application.

Practiced:

- DOM element selection
- event listener setup
- form submission handling
- input value extraction
- validation logic
- early return patterns

Implemented initial submission handling using:

```js
addEventListener("submit", ...)
```

Strengthened understanding of:

- `preventDefault()`
- `.value`
- `.trim()`
- DOM references
- event flow
- callback execution timing
- truthy/falsy validation
- primitive values vs DOM elements

Reviewed:

- `querySelector()` vs `getElementById()`
- selector caching
- DOM manipulation concepts
- separation of structure, presentation, and behavior

---

## Current Todo App Status

Current implementation includes:

- semantic HTML structure
- modular file organization
- form submission interception
- DOM element selection
- trimmed input validation
- empty submission prevention

Next planned steps:

- dynamic todo item creation
- DOM appending
- input clearing
- empty state toggling
- delete functionality
- completed task states
- localStorage integration
