# Dev Log

## Linux VPS / Infrastructure

### SSH & User Management

- Configured SSH key authentication for iPad access using Termius
- Successfully authenticated into VPS from iPad without password prompts
- Disabled password authentication for SSH access (`PasswordAuthentication no`)
- Verified password authentication was disabled correctly using `grep`
- Learned how SSH authentication works with public/private keypairs
- Discussed security implications of disabling password authentication
- Created secondary non-sudo development user: `benji`
- Tested user switching with `su - benji` and `su - benjibusco`
- Learned difference between user passwords and privilege escalation workflows
- Learned that `exit` cleanly returns from nested `su` shell sessions
- Discussed separation between:
  - infrastructure/admin account (`benjibusco`)
  - daily development account (`benji`)

---

## Linux Permissions & Groups

### Shared Group Architecture

- Created shared development group: `webdev`
- Added `benji` to `webdev` using `usermod -aG webdev benji`
- Learned:
  - purpose of supplementary groups
  - meaning of `-aG`
  - difference between primary and supplementary groups
  - why Linux creates a user group matching the username

### Ownership & Permission Standardization

- Standardized active web environments using:
  - owner → `benjibusco`
  - group → `webdev`
- Applied recursive ownership updates using:
  - `chown -R benjibusco:webdev`
- Configured deploy script ownership and execution permissions
- Learned:
  - meaning of `-R` (recursive)
  - difference between owner/group permissions
  - how setgid inheritance works
  - why shared groups are cleaner than using `777`

### Permission Learning

- Deep dive into Linux permission numbers:
  - `755`
  - `775`
  - `664`
  - `700`
  - `600`
- Learned:
  - read/write/execute values (`4/2/1`)
  - why `777` is dangerous
  - why `775` is appropriate for collaborative environments
  - why HTML files should not be executable
- Fixed executable permissions on `index.html`
- Learned that:
  - group members can edit files
  - but only owner/root can change file permissions
- Learned file truncation workflow using:
  - `> file.txt`

### Current Environment Structure

```text
Owner  -> benjibusco
Group  -> webdev
Dev    -> benji
```

---

## Git & GitHub

### Git Initialization

- Initialized Git repository inside:
  - `/var/www/lab.benjibusco.cloud/app`
- Created:
  - `.gitignore`
  - `README.md`
- Renamed default branch to `main`
- Learned:
  - difference between local commits and remote pushes
  - commit timestamps vs push timestamps
  - how Git generates commit hashes

### Git Identity

- Configured global Git identity:
  - `user.name=Benji`
  - `user.email=kralbenji@gmail.com`
- Discussed:
  - Git identity vs Linux usernames
  - GitHub usernames vs Git author metadata
  - benefits of conventional commit messages

### GitHub SSH Authentication

- Generated GitHub SSH deploy key using:
  - `ssh-keygen -t ed25519`
- Added deploy key to GitHub repository:
  - `KralBenji/lab`
- Successfully authenticated VPS with:
  - `ssh -T git@github.com`
- Connected GitHub remote using SSH:
  - `git@github.com:KralBenji/lab.git`
- Successfully pushed initial commits to GitHub

### Conventional Commit Workflow

- Standardized commit style using prefixes:
  - `feat:`
  - `fix:`
  - `refactor:`
  - `docs:`
  - `style:`
  - `chore:`

---

## Lab Ecosystem Architecture

### Infrastructure Structure

Created scalable lab ecosystem structure:

```text
/var/www/lab.benjibusco.cloud/
├── app/
├── backups/
├── deploy/
└── logs/
```

### Public App Structure

Created expandable public application structure:

```text
app/
├── dashboard/
├── tools/
├── sandbox/
├── status/
├── assets/
├── styles/
│   ├── reset.css
│   ├── variables.css
│   ├── base.css
│   ├── layout.css
│   └── components.css
└── scripts/
    └── main.js
```

### Dashboard Development

- Built cinematic dark-mode dashboard homepage
- Implemented:
  - hero section
  - reusable card system
  - responsive dashboard grid
  - scalable modular CSS architecture
- Created section navigation for:
  - Dashboard
  - Tools
  - Sandbox
  - Status

### Design System & Styling

- Implemented reusable token-based design system
- Added:
  - spacing tokens
  - radius tokens
  - accent tokens
  - gradient tokens
- Refined dashboard aesthetic toward:
  - cinematic dark mode
  - premium developer-tool feel
  - blue/purple branded atmosphere
- Combined aesthetic inspiration from:
  - benkelly.design accent blue
  - kralbenji purple atmosphere
- Implemented:
  - layered gradients
  - ambient glow effects
  - polished card gradients
  - improved visual depth and separation

### Deployment Workflow

Created manual VPS deployment workflow:

```text
VS Code Local
    ↓
git commit
    ↓
git push
    ↓
SSH into VPS
    ↓
./deploy.sh
    ↓
Live site updates
```

Created deployment script:

```text
/var/www/lab.benjibusco.cloud/deploy/deploy.sh
```

Implemented:

- executable deployment permissions
- shared group execution access
- manual Git pull deployment process

### Architecture Decisions

- Separated public app layer from infrastructure layer
- Discussed future scalability for:
  - Node.js
  - APIs
  - React/Vite
  - deployment scripts
  - logging systems
  - backup systems
  - dashboards
  - server monitoring

---

## Local Development Environment

### Workspace Organization

Standardized local workspace structure:

```text
Documents/
└── Development/
```

Migrated active repositories:

- benkelly
- kralbenji
- ben-dev-log
- lab

Verified:

- Git history remained intact
- remotes remained connected
- branches remained functional
- VS Code integration worked correctly

### VS Code Workflow

- Configured terminal-based VS Code launching using:
  - `code .`
- Learned local workflow for:
  - cloning repositories
  - opening projects from terminal
  - editing locally in VS Code
  - deploying to VPS

---

## Linux Learning Progress

### Commands & Concepts Learned

Became comfortable using and understanding:

- `chmod`
- `chown`
- `chgrp`
- `usermod`
- `cd ..`
- `pwd`
- `git init`
- `git status`
- `git add`
- `git commit`
- `git push`
- `git pull`
- `rm`
- `touch`
- `nano`
- `cat`

### Key Conceptual Breakthroughs

- Connected Linux ownership model:
  - `owner:group`
- Understood relationship between:
  - users
  - groups
  - permissions
  - inheritance
- Learned how Git trust/safe.directory works in shared environments
- Developed deeper understanding of scalable Linux architecture
- Became more comfortable with terminal-first development workflows

---

## Notes

- Began intentionally structuring VPS environments more like real-world development infrastructure
- Shifted focus from “just making it work” toward understanding systems deeply
- Continued building Linux confidence through manual command usage and conceptual learning
- Dashboard project evolved from simple test page into scalable internal developer environment
