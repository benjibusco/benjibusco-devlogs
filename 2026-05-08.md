# Dev Log — May 8, 2026

# Focus for Today

Continue building the lab ecosystem while strengthening Linux administration, VPS architecture, Git workflows, SSH security, permissions management, and deployment infrastructure.

---

# SSH + User Management

Configured SSH key authentication for iPad access using Termius and successfully authenticated into the VPS without password prompts.

Disabled SSH password authentication using `PasswordAuthentication no` and verified the configuration using `grep`.

Created a secondary non-sudo development user: `benji`.

Discussed separation between:

- infrastructure/admin account (`benjibusco`)
- daily development account (`benji`)

Practiced user switching with `su - benji` and `su - benjibusco` while learning nested shell workflows and privilege separation concepts.

---

# Linux Permissions + Groups

Created a shared development group: `webdev`.

Added `benji` to the group using:

```bash
usermod -aG webdev benji
```

Standardized environment ownership using:

```text
Owner  -> benjibusco
Group  -> webdev
Dev    -> benji
```

Applied recursive ownership updates using:

```bash
chown -R benjibusco:webdev
```

Configured deploy script ownership, shared group permissions, and setgid inheritance.

Learned:

- supplementary vs primary groups
- recursive ownership behavior
- setgid inheritance
- why shared groups are cleaner than `777`

Deepened understanding of Linux permissions including:

- `755`
- `775`
- `664`
- `700`
- `600`

Learned read/write/execute values (`4/2/1`), why `777` is dangerous, why HTML files should not be executable, and how Linux separates permission ownership from file editing access.

Fixed executable permissions on `index.html` and practiced file truncation using:

```bash
> file.txt
```

---

# Git + GitHub

Initialized a Git repository inside:

```text
/var/www/lab.benjibusco.cloud/app
```

Created:

- `.gitignore`
- `README.md`

Renamed the default branch to `main`.

Configured global Git identity:

- `user.name=Benji`
- `user.email=kralbenji@gmail.com`

Discussed differences between Linux usernames, Git author metadata, and GitHub usernames.

Generated a GitHub SSH deploy key using:

```bash
ssh-keygen -t ed25519
```

Added the deploy key to `KralBenji/lab`, authenticated using:

```bash
ssh -T git@github.com
```

Connected the GitHub remote using SSH and pushed the initial lab commits successfully.

Standardized commit naming using:

- `feat:`
- `fix:`
- `refactor:`
- `docs:`
- `style:`
- `chore:`

---

# Lab Ecosystem Architecture

Created scalable VPS structure:

```text
/var/www/lab.benjibusco.cloud/
├── app/
├── backups/
├── deploy/
└── logs/
```

Created expandable app structure:

```text
app/
├── dashboard/
├── tools/
├── sandbox/
├── status/
├── assets/
├── styles/
└── scripts/
```

Built the first dark-mode dashboard homepage with:

- hero section
- reusable card system
- responsive dashboard grid
- modular CSS architecture

Added section navigation for:

- Dashboard
- Tools
- Sandbox
- Status

Implemented a reusable token-based design system with 'spacing tokens, radius tokens, accent tokens, and gradient tokens'.

Refined the visual system using 'cinematic dark mode, layered gradients, ambient glow effects, polished card gradients, and blue/purple branded styling'.

---

# Deployment Workflow

Created the first manual VPS deployment workflow:

```text
VS Code Local
    ↓
git commit
    ↓
git push
    ↓
SSH into VPS
    ↓
./deploy.sh
    ↓
Live site updates
```

Created:

```text
/var/www/lab.benjibusco.cloud/deploy/deploy.sh
```

Configured executable deployment permissions, shared group execution access, and manual Git pull deployment workflows.

---

# Local Development Environment

Standardized the local workspace structure:

```text
Documents/
└── Development/
```

Migrated repositories:

- benkelly
- kralbenji
- ben-dev-log
- lab

Verified Git history, remotes, branches, and VS Code integration remained intact.

Configured terminal-based VS Code launching using:

```bash
code .
```

---

# Linux Learning Progress

Strengthened understanding of:

- `owner:group` relationships
- users/groups/permissions
- inheritance
- Git `safe.directory`
- terminal-first workflows
- scalable Linux infrastructure architecture
- everyday Linux commands
