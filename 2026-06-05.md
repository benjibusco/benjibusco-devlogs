# Dev Log — June 5, 2026

### Focus for Today

Refactored the deployment monitoring system into a fully environment-aware architecture supporting clean staging-first workflows, isolated deployment metadata, PM2 runtime monitoring, and merge-safe deployment infrastructure between production and staging environments.

Expanded VPS operational redundancy through authenticated phone-based SSH recovery access and reorganized long-term engineering planning documentation into a scalable domain-based architecture.

## VPS Infrastructure

### Multi-Device SSH Redundancy

Expanded VPS SSH access to include phone-based Termius authentication for emergency infrastructure access and deployment recovery.

The SSH environment now supports authenticated access from:

- MacBook
- desktop
- iPad
- phone

Configured a dedicated phone SSH identity using an ED25519 key and added the public key to the VPS `authorized_keys` system.

Further standardized the infrastructure authentication model around:

- per-device SSH keys
- passwordless public key authentication
- multi-device redundancy
- deployment account isolation

Discussed long-term operational value of phone-based SSH access for:

- emergency production fixes
- PM2/API restarts
- Apache troubleshooting
- SSL renewal failures
- CI/CD deployment recovery
- lockout prevention
- remote monitoring

This reinforced broader infrastructure concepts including:

- SSH architecture
- operational redundancy
- deployment recovery workflows
- Linux administration practices
- infrastructure resiliency planning

## Documentation Architecture

### Domain-Based Planning Organization

Reorganized long-term planning documentation into a more scalable domain-based architecture inside the `plans/` directory.

Began transitioning from broad topic folders into engineering-oriented system categories including:

- infrastructure
- frontend
- architecture
- sandbox
- observability

### Long-Term Planning Structure

Planned future separation of:

- active plans
- archived plans
- completed initiatives

to support continued ecosystem growth and improve long-term maintainability of deployment, authentication, dashboard, and frontend planning documentation.

Reviewed naming consistency across planning systems and identified `infrastructure/` as a more scalable replacement for the previous `vps/` category as the lab ecosystem expands beyond VPS-only concerns into broader platform engineering workflows.

## Deployment Infrastructure

### Environment-Aware API Architecture

Refactored the API monitoring system so production and staging environments can share the same codebase without overwriting environment-specific paths or runtime configuration.

Implemented runtime environment detection using filesystem path checks such as:

```js id="j0mjlwm"
const isStaging = __dirname.includes("/api.dev.benjibusco.cloud/");
```

Used this pattern throughout the deployment system to dynamically configure:

- deployment metadata directories
- API roots
- PM2 process names
- deployment branch labels
- monitoring environment labels
- frontend API endpoints

This significantly improved long-term maintainability of the deployment architecture and reduced merge conflicts between staging and production branches.

### Deployment Metadata Improvements

Expanded deployment metadata written by `deploy-lib.sh`.

Added support for:

- commit messages
- GitHub commit URLs
- full commit hashes
- short commit hashes
- deployment durations
- deployment timestamps

Deployment cards can now display:

- clickable commit hashes
- commit messages
- deployment timing information
- PM2 runtime state

Implemented GitHub commit URL generation using:

```bash id="dczp74x"
https://github.com/${GITHUB_OWNER}/${PROJECT}/commit/${commit_full}
```

Updated GitHub owner references from:

```text id="v5bhyq9"
KralBenji
```

to:

```text id="7rgf5a6"
benjibusco
```

after repository namespace migration.

### PM2 Runtime Monitoring

Integrated PM2 runtime monitoring into both:

- `/status`
- `/deployments`

Added runtime process visibility for:

- production API
- staging API

Deployment cards now support:

- PM2 process name
- PM2 online/offline status
- PM2 restart counts

Implemented PM2 process lookup using:

```bash id="5hz0lcl"
pm2 jlist
```

and attached process metadata dynamically during deployment API responses.

### Merge-Friendly Deployment Architecture

Refactored deployment scripts into a merge-safe environment-aware structure.

Updated:

- `server.js`
- `routes/deploy.js`
- `deploy-lib.sh`
- deployment wrapper scripts

to support clean staging → main merge workflows.

Removed hardcoded deployment library sourcing such as:

```bash id="q2fxnqn"
source "/var/www/api.benjibusco.cloud/app/scripts/deployments/deploy-lib.sh"
```

and replaced it with runtime-relative sourcing:

```bash id="krpjlwm"
source "$SCRIPT_DIR/deploy-lib.sh"
```

This allows:

- isolated staging deployments
- isolated production deployments
- identical deployment logic across environments
- lower merge conflict risk
- cleaner branch synchronization workflows

### Deployment Script Standardization

Standardized deployment wrappers for:

- lab
- api
- benkelly
- kralbenji

across both:

- production
- staging

Updated deployment wrappers to:

- use runtime-relative deploy-lib sourcing
- support environment-aware deployment roots
- isolate deployment JSON generation
- isolate deployment logs
- preserve environment-specific site paths

Corrected staging API deployment pathing from:

```text id="rq0q6nl"
/public_html
```

to:

```text id="uv3uqkr"
/app
```

for proper Node.js deployment structure consistency.

## Frontend Monitoring Systems

### Frontend Environment-Aware Status System

Updated frontend status monitoring to dynamically switch between:

- production API endpoints
- staging API endpoints

based on the current hostname.

Implemented runtime API base selection:

```js id="z4mjlwm"
const isStaging = window.location.hostname.includes("dev.benjibusco.cloud");
```

This allows:

```text id="qk8y8zw"
lab.benjibusco.cloud
→ production API
```

and:

```text id="9z8j0wb"
lab.dev.benjibusco.cloud
→ staging API
```

without maintaining separate frontend codebases.

### Staging Environment Migration

Migrated staging API monitoring away from production deployment metadata into isolated staging deployment metadata.

Created staging deployment metadata directories:

```text id="b9ljv1h"
/var/www/api.dev.benjibusco.cloud/app/data/deployments
```

Generated staging deployment JSON files for all monitored targets:

- lab production
- lab staging
- api production
- api staging
- benkelly production
- benkelly staging
- kralbenji production
- kralbenji staging

Verified staging dashboard successfully rendered all deployment cards using staging API data.

## Git Workflow Architecture

### Staging Branch Synchronization

Merged `main` into staging branches for both:

- lab
- api

to synchronize:

- deployment monitoring upgrades
- PM2 monitoring
- environment-aware architecture
- deployment metadata improvements

Resolved merge conflicts caused by earlier hardcoded environment values and confirmed the new architecture substantially reduces future merge friction.

## Infrastructure Planning

### GitHub Governance Planning

Created a long-term branch protection workflow plan for future GitHub repository governance improvements.

Planned protections for:

- `main`
- `staging`

including:

- pull request requirements
- force-push prevention
- branch deletion protection
- deployment validation workflows

Created planning category:

```text id="x6g9vru"
docs/plans/infrastructure/github/
```

to support future GitHub governance and repository workflow documentation.
