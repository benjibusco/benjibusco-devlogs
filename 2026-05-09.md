# Dev Log

## Linux VPS / Infrastructure

### Staging Environment Refactor

- Refactored staging architecture to mirror production environments instead of nested subfolder staging
- Replaced:
  - `dev.benjibusco.cloud/kralbenji/`
  - `dev.benjibusco.cloud/benkelly/`
- Adopted dedicated staging subdomains:
  - `kralbenji.dev.benjibusco.cloud`
  - `benkelly.dev.benjibusco.cloud`

Improved:

- clean URL parity
- deployment consistency
- Apache routing simplicity
- scalability

### New VPS Environment Structure

Created new environment web roots:

```bash
sudo mkdir -p /var/www/kralbenji.dev.benjibusco.cloud/public_html
sudo mkdir -p /var/www/benkelly.dev.benjibusco.cloud/public_html
sudo mkdir -p /var/www/api.benjibusco.cloud/public_html
sudo mkdir -p /var/www/api.dev.benjibusco.cloud/public_html
```

Current environment structure:

```text
Production
├── kralbenji.com
├── benkelly.design
└── api.benjibusco.cloud

Staging
├── kralbenji.dev.benjibusco.cloud
├── benkelly.dev.benjibusco.cloud
└── api.dev.benjibusco.cloud

Infrastructure
└── lab.benjibusco.cloud
```

### Shared Environment Standardization

Applied standardized ownership model:

```text
Owner  -> benjibusco
Group  -> webdev
Dev    -> benji
```

Configured:

- shared group permissions
- recursive ownership
- setgid inheritance (`chmod 2775`)

### API Environment Initialization

Created API environment structure:

```text
/var/www/api.benjibusco.cloud/
├── public_html/
├── logs/
├── backups/
└── deploy/
```

Created initial API landing page and validated Apache delivery:

```bash
curl -I https://api.benjibusco.cloud
```

Confirmed:

```text
HTTP/1.1 200 OK
```

---

## Git & GitHub

### Staging Repository Deployment

Deployed staging branches into:

```text
/var/www/kralbenji.dev.benjibusco.cloud/public_html
/var/www/benkelly.dev.benjibusco.cloud/public_html
```

Configured:

- Git safe directories
- staging synchronization workflow
- VPS → GitHub SSH deployment access

### Branch Synchronization Workflow

Merged `main` into `staging`.

Validated:

- local ↔ GitHub ↔ VPS synchronization
- staging deployment integrity
- clean URL consistency

Used:

```bash
git fetch origin
git reset --hard origin/staging
```

### API Repository Initialization

Initialized Git repository for:

```text
/var/www/api.benjibusco.cloud
```

Configured:

- `main` branch standardization
- Git safe directory configuration
- `.gitignore` exclusions:
  - logs
  - backups
  - deploy scripts
- shared Git identity configuration

---

## Apache / SSL / Routing

### Dedicated Staging VHosts

Created Apache vhost configurations for:

- `kralbenji.dev.benjibusco.cloud`
- `benkelly.dev.benjibusco.cloud`

Configured:

- dedicated `DocumentRoot`
- isolated logs
- `AllowOverride All`
- clean URL support
- SSL compatibility

### SSL / HTTPS

Generated SSL certificates using Certbot for staging environments.

Validated:

- HTTPS routing
- Apache SSL configs
- `.htaccess` compatibility
- clean URLs over HTTPS

### API SSL Deployment

Generated SSL certificate for:

```text
api.benjibusco.cloud
```

Configured:

- dedicated Apache API vhost
- isolated API logs
- HTTPS routing

Validated:

- successful HTTPS responses
- Apache vhost recognition
- API routing

### Production Routing Recovery

Recovered production routing after `.htaccess` rewrite issues.

Validated routing using:

```bash
curl -I
```

Confirmed:

```text
/about → 200 OK
/pages/about.html → 301 redirect
```

---

## Dashboard / Infrastructure UI

### Internal Developer Dashboard

Continued building internal dashboard for:

```text
lab.benjibusco.cloud
```

Implemented:

- reusable environment card system
- production/staging environment grouping
- API service grouping
- development utility sectioning

Added:

- favicon/icon support for environment cards
- standardized card header structure
- shared icon container system

Enhanced cards with:

- hover elevation
- glow effects
- shadow transitions

Planned:

- infrastructure monitoring
- deployment tracking
- status indicators
- tooling widgets

---

## Front-End / Navigation Refactor

### Root-Relative Navigation Standardization

Standardized navigation using root-relative clean URLs.

### Navigation Standard

```html
<a href="/">Home</a>
<a href="/about">About</a>
<a href="/music">Music</a>
<a href="/shop">Shop</a>
<a href="/contact">Contact</a>
```

Applied across:

- index.html
- about.html
- music.html
- shop.html
- contact.html

Reinforced clean URL architecture:

```text
/about → /pages/about.html
```

---

## Architecture Decisions

- Standardized staging environments to mirror production structure
- Separated:
  - applications
  - APIs
  - staging environments
  - infrastructure tooling
- Began separating:
  - development workflows
  - deployment ownership
  - infrastructure management

---

## Notes

- Production and staging environments now behave consistently
- API environment now includes:
  - SSL
  - Apache isolation
  - Git tracking
  - scalable directory structure
- Continued strengthening understanding of:
  - Apache
  - Git workflows
  - VPS architecture
  - deployment systems
  - SSL management
  - environment parity

### Automated VPS Deployment Workflows

Implemented GitHub Actions deployment pipelines for:

```text
api.benjibusco.cloud
lab.benjibusco.cloud
```

Configured:

- dedicated GitHub Actions deployment SSH key
- VPS SSH authentication for CI/CD workflows
- repository-level GitHub Actions secrets
- automated `git pull` deployment workflows

Deployment flow:

```text
Local Development
→ Git Push
→ GitHub Actions
→ VPS Auto Deploy
→ Live Environment Updated
```

Created deployment workflow structure:

```text
.github/workflows/deploy.yml
```

Validated:

- automated VPS SSH authentication
- GitHub Actions deployment execution
- remote Git synchronization
- production deployment automation

---

## Dashboard / Infrastructure UI

### Infrastructure Status System

Implemented reusable infrastructure status badge system.

Added support for:

- production environment indicators
- staging environment indicators
- future offline environment indicators

Implemented:

- reusable `.status-badge` component
- modular status color variants
- infrastructure environment labeling
- environment header alignment system

Added visual indicators for:

```text
Online
Staging
Offline
```

Enhanced dashboard hierarchy with:

- structured card title grouping
- reusable environment metadata layout
- infrastructure-focused UI organization

### API Services Interface Refactor

Refactored API environment page to function as infrastructure tooling interface instead of duplicated dashboard navigation.

Replaced duplicated environment overview cards with deeper API service structure:

```text
/auth
/docs
/monitoring
/versioning
```

Added:

- API service categorization
- infrastructure tooling placeholders
- developer resource structure
- navigation links back to dashboard and landing environment

### Mobile-First Dashboard Component Refactor

Refactored shared dashboard component system using mobile-first architecture.

Improved:

- card header responsiveness
- status badge alignment
- environment metadata scaling
- reusable component organization

Standardized:

- card header layout
- icon wrapper structure
- section header hierarchy
- reusable status indicator system
