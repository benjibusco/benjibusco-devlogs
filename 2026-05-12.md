# 📅 Devlog — May 12, 2026

## 🧠 Focus for Today

Transitioned the lab status system from a frontend-only mock dashboard into a fully deployed live infrastructure monitoring system powered by Node.js, Express, PM2, Apache reverse proxying, live VPS metrics, dynamic service health checks, and automated CI/CD deployment tracking.

---

## ✅ Tasks Completed

### 🌐 Express API Deployment

- Installed PM2 globally for Node.js process management
- Started Express API as a managed PM2 service: `benjibusco-api`
- Configured PM2 process persistence across server restarts
- Verified API health using `pm2 status` and `curl http://localhost:3000/status`

---

### 🔀 Apache Reverse Proxy Configuration

- Enabled Apache proxy modules:
  - `proxy`
  - `proxy_http`
- Configured Apache reverse proxy for `https://api.benjibusco.cloud`
- Routed HTTPS traffic to the internal Express server running on `localhost:3000`
- Preserved SSL integration using existing Certbot certificates
- Verified successful public HTTPS API responses

---

### 🔐 Frontend ↔ Backend Integration

- Updated lab dashboard API endpoint from `/data/status.json` to `https://api.benjibusco.cloud/status`
- Implemented CORS support in Express using `app.use(cors())`
- Restarted PM2 process after backend configuration changes
- Successfully connected live frontend dashboard to backend API service

---

### 📊 Live VPS Monitoring

- Replaced placeholder infrastructure metrics with live VPS data
- Implemented live uptime tracking using `os.uptime()`
- Added formatted uptime conversion helper for days, hours, and minutes
- Added live RAM usage monitoring using Node OS memory APIs
- Added live disk usage monitoring using Linux `df` command execution through Node child processes
- Successfully rendered real-time VPS metrics directly into dashboard resource cards

---

### 🩺 Live Service Health Checks

Refactored multiple infrastructure services from hardcoded placeholder values into live backend-driven health checks.

Implemented live monitoring for:

- Apache service state
- PM2 Node.js API process state
- HTTPS/SSL availability
- deployment status tracking

Added reusable helper functions:

- `checkService()`
- `checkPm2Process()`
- `checkSslEndpoint()`
- `getDeploymentStatus()`

Implemented Linux service verification using `systemctl is-active`

Implemented PM2 process monitoring using `pm2 describe`

Implemented live HTTPS availability checks against the public API domain to verify SSL accessibility without directly exposing certificate file permissions.

Added deployment state tracking through `data/deployments/latest.json`

The backend API now dynamically orchestrates infrastructure status data instead of relying on frontend hardcoded values.

---

### 🚀 Deployment Metadata System

Expanded deployment tracking from simple status indicators into a metadata-driven deployment monitoring system.

Added deployment metadata support for:

- deployment name
- environment
- branch
- commit SHA
- deployment timestamp
- deployment message

Updated the frontend deployment card to dynamically render:

- latest deployment name
- active branch
- deployment commit SHA
- deployment timestamp

Added new deployment rendering IDs to the status dashboard and refactored `status.js` to support deployment metadata states and fallback handling.

Implemented frontend commit SHA shortening using `slice(0, 7)` for cleaner GitHub-style deployment rendering.

---

### 🤖 Automated GitHub Actions Deployment Tracking

Integrated deployment monitoring directly into the lab CI/CD pipeline.

Updated `.github/workflows/deploy.yml` to automatically generate and update `/var/www/api.benjibusco.cloud/app/data/deployments/latest.json` after successful deployments.

Implemented automated deployment metadata generation using `cat <<EOF` within the GitHub Actions SSH deployment workflow.

Deployment automation now dynamically injects:

- Git branch
- commit SHA
- deployment timestamp
- deployment status
- deployment message

directly into the infrastructure monitoring system.

Configured deployment timestamps to use `America/New_York` timezone formatting for consistent Eastern time rendering across the dashboard and API.

---

## 🧠 Concepts Learned

- PM2 process orchestration
- Apache reverse proxy architecture
- HTTPS backend routing
- CORS handling between frontend/backend services
- Node.js OS module usage
- Child process execution in Node.js
- Linux service state monitoring
- PM2 process inspection
- Infrastructure observability concepts
- Deployment metadata architecture
- GitHub Actions workflow automation
- Dynamic CI/CD deployment tracking
- Backend-driven dashboard rendering
- Real-time infrastructure monitoring workflows
- Public API deployment architecture
- Full-stack frontend ↔ backend integration pipeline

---

## 🚀 Result

Successfully deployed the first live backend-powered infrastructure monitoring system for the Benjibusco lab ecosystem featuring:

- Express API
- PM2-managed backend service
- Apache reverse proxy
- HTTPS public API routing
- Live frontend polling
- Real VPS uptime
- Live RAM usage
- Live disk usage
- Dynamic service health checks
- Automated deployment tracking
- GitHub Actions CI/CD integration
- Live deployment metadata rendering
- Deployment observability tooling
- Dynamic infrastructure dashboard rendering

The lab ecosystem has now evolved from:

```text
Static Frontend Prototype
```

into:

```text
Full-Stack Infrastructure Monitoring Platform
```
