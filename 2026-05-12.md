# Dev Log — May 12, 2026

# Focus for Today

Transition the lab status system from a frontend-only prototype into a live backend-powered infrastructure monitoring platform using Node.js, Express, PM2, Apache reverse proxying, live VPS metrics, and CI/CD deployment tracking.

---

# Express API Deployment

Installed PM2 globally and started the Express API as a managed PM2 service: `benjibusco-api`.

Configured PM2 persistence across server restarts and verified API health using `pm2 status` and `curl http://localhost:3000/status`.

---

# Apache Reverse Proxy Configuration

Enabled Apache `proxy` and `proxy_http` modules.

Configured `https://api.benjibusco.cloud` as a reverse proxy routing HTTPS traffic to the internal Express server running on `localhost:3000`.

Preserved existing Certbot SSL integration and verified successful public HTTPS API responses.

---

# Frontend ↔ Backend Integration

Updated the lab dashboard API endpoint from `/data/status.json` to `https://api.benjibusco.cloud/status`.

Integrated CORS support using `app.use(cors())`, restarted the PM2 process after backend changes, and connected the frontend dashboard directly to the backend API service.

---

# Live VPS Monitoring

Replaced placeholder infrastructure metrics with live VPS data.

Implemented:

- live uptime tracking using `os.uptime()`
- RAM usage monitoring using Node OS memory APIs
- disk usage monitoring using Linux `df` command execution through Node child processes

Added formatted uptime helpers for days, hours, and minutes while rendering real-time VPS metrics directly into dashboard resource cards.

---

# Live Service Health Checks

Refactored infrastructure services from hardcoded placeholder values into backend-driven health checks.

Implemented monitoring for:

- Apache service state
- PM2 Node.js API process state
- HTTPS/SSL availability
- deployment status tracking

Added reusable helpers:

- `checkService()`
- `checkPm2Process()`
- `checkSslEndpoint()`
- `getDeploymentStatus()`

Implemented Linux service verification using `systemctl is-active`, PM2 inspection using `pm2 describe`, and HTTPS endpoint verification against the public API domain.

Added deployment state tracking through `/data/deployments/latest.json`.

The backend API now dynamically orchestrates infrastructure status data instead of relying on frontend hardcoded values.

---

# Deployment Metadata System

Expanded deployment tracking into a metadata-driven monitoring system.

Added deployment metadata support for 'deployment name, environment, branch, commit SHA, deployment timestamp, and deployment message'.

Updated the frontend deployment card to dynamically render:

- latest deployment name
- active branch
- deployment commit SHA
- deployment timestamp

Added new deployment rendering IDs to the dashboard and refactored `status.js` to support deployment metadata states and fallback handling.

Implemented frontend commit SHA shortening using `slice(0, 7)` for cleaner GitHub-style rendering.

---

# Automated GitHub Actions Deployment Tracking

Integrated deployment monitoring directly into the CI/CD pipeline.

Updated `.github/workflows/deploy.yml` to automatically generate and update:

```text
/var/www/api.benjibusco.cloud/app/data/deployments/latest.json
```

Implemented deployment metadata generation using `cat <<EOF` inside the GitHub Actions SSH deployment workflow.

Deployment automation now injects 'Git branch, commit SHA, deployment timestamp, deployment status, and deployment message' directly into the monitoring system.

Configured deployment timestamps to use `America/New_York` timezone formatting for consistent Eastern time rendering.

---

# Result

Successfully deployed the first live backend-powered infrastructure monitoring system for the lab ecosystem featuring:

- Express API
- PM2-managed backend services
- Apache reverse proxying
- HTTPS API routing
- live frontend polling
- real VPS metrics
- dynamic service health checks
- automated deployment tracking
- GitHub Actions CI/CD integration
- deployment metadata rendering

The lab evolved from:

```text
Static Frontend Prototype
```

into:

```text
Full-Stack Infrastructure Monitoring Platform
```
