# Dev Log — May 31, 2026

### Focus for Today

Complete production browser authentication integration for Benjibusco Lab while expanding the infrastructure into a production-style multi-environment deployment architecture.

Focused on:

- frontend ↔ VPS auth integration
- production HTTPS browser authentication
- secure cookie validation
- protected route architecture
- auth navigation UX improvements
- staging environment architecture
- automated CI/CD deployment
- production and staging separation
- deployment workflow refinement

## Authentication Infrastructure

### Auth Testing Cleanup

Removed temporary curl testing credentials from the API environment:

```bash
rm login-test.json
```

Confirmed temporary curl helper files were no longer required after successful browser-based authentication validation.

### Production Browser Authentication Validation

Successfully validated the full production authentication stack inside a real browser environment.

Verified complete authentication flow:

```text
Browser
↓
lab.benjibusco.cloud
↓
HTTPS fetch requests
↓
api.benjibusco.cloud
↓
Apache reverse proxy
↓
Express auth middleware
↓
PostgreSQL sessions
```

Confirmed working:

- `/auth/login`
- `/auth/me`
- `/auth/logout`
- `credentials: "include"`
- secure HTTPS cookies
- PostgreSQL session persistence
- frontend tab sessions
- protected route enforcement
- browser refresh persistence
- logout flow
- PM2 production API stability

### Reverse Proxy + Secure Cookie Validation

Diagnosed and resolved secure session cookie issues during live HTTPS testing.

Added trusted proxy handling:

```js
app.set("trust proxy", 1);
```

Verified production Express session behavior:

```text
secure: true
protocol: https
forwardedProto: https
```

Confirmed successful secure cookie issuance:

```text
Set-Cookie: connect.sid
Secure
HttpOnly
SameSite=Strict
```

Validated authenticated requests through:

```text
https://api.benjibusco.cloud
```

instead of localhost-only curl testing.

## Frontend Authentication Architecture

### Frontend ↔ Production API Integration

Updated the frontend auth system to use the live production API.

Refactored:

```js
const API_BASE_URL = "https://api.benjibusco.cloud";
```

Removed localhost-only API usage from the frontend authentication layer.

Verified browser authentication worked correctly against the production VPS environment.

### Auth Route Architecture Cleanup

Refactored route protection flow to better support scalable protected page routing.

Updated authentication behavior to preserve:

- intended protected destination
- originating public page

Introduced query parameter architecture:

```text
/login/?redirect=...&return=...
```

Purpose:

```text
redirect
→ protected page after login

return
→ originating public page if login is cancelled
```

This improved protected navigation behavior across:

- dashboard
- tools
- sandbox
- freelance pages

### Login Navigation UX Improvements

Implemented dynamic login return behavior.

Examples:

```text
/dashboard/
↓
/tools/
↓
/login/?redirect=/tools/&return=/dashboard/
```

Result:

```text
Return
→ /dashboard/

Successful Login
→ /tools/
```

Also implemented equivalent behavior for:

```text
/sandbox/
→ /sandbox/freelance/
```

This created a significantly cleaner protected-route user experience.

### Protected Route Architecture Refactor

Removed the old dedicated tools private subfolder structure:

```text
/tools/private/
```

Migrated to:

```text
/tools/
```

as the directly protected route.

Benefits:

- cleaner URL structure
- simpler auth flow
- scalable protected routing model
- reduced nested routing complexity

Updated all frontend links and auth redirects accordingly.

Removed all remaining references to:

```text
/tools/private/
```

### Freelance Authentication Integration

Integrated authentication behavior into the freelance environment.

Added:

- logout button
- auth scripts
- protected route support
- shared auth UI behavior

Maintained universal authentication architecture across:

```text
/tools/
/sandbox/freelance/
```

instead of using tool-specific auth systems.

### Shared Auth Component Refactor

Moved logout button styling from page-specific styles into reusable shared component styling.

Migrated:

```css
.auth-logout-button
```

into:

```text
styles/components.css
```

This aligned the auth UI system with the broader reusable Lab component architecture.

### Production Browser Validation

Completed final live browser testing after deployment.

Validated:

- tools page protection
- freelance page protection
- login redirects
- return navigation
- logout behavior
- protected route redirects
- refresh persistence
- authenticated navigation persistence

Confirmed:

```text
Production authentication is now fully operational.
```

## Git + Deployment Workflows

### Git Workflow Improvements

Continued refining production-style Git workflows.

Used:

- isolated feature branches
- merge workflows
- production deployment validation
- GitHub Actions deployment automation

Feature branches used:

```text
feat/auth-integration
feat/auth-navigation
```

Expanded workflow architecture into a production-style staging environment.

## Staging Environment Architecture

### Staging Environment Architecture

Created a dedicated staging environment for the lab ecosystem using:

```text
lab.dev.benjibusco.cloud
```

Established a mirrored VPS structure matching the production environment.

Production:

```text
/var/www/lab.benjibusco.cloud/
```

Staging:

```text
/var/www/dev.benjibusco.cloud/lab/
```

Created mirrored staging directories:

```text
lab/
├── app/
├── backups/
├── deploy/
└── logs/
```

Maintained consistency with the existing staging ecosystem:

- `benkelly.dev.benjibusco.cloud`
- `kralbenji.dev.benjibusco.cloud`

### Git Branch Staging Workflow

Created a dedicated staging branch:

```bash
git checkout -b staging
```

Configured the staging VPS environment to track:

```text
origin/staging
```

Verified branch tracking using:

```bash
git branch
git status
git remote -v
```

Confirmed isolated deployment architecture:

```text
Production
→ branch: main

Staging
→ branch: staging
```

This created a safe pre-production workflow for:

- authentication rollout testing
- deployment validation
- infrastructure experimentation
- UI and layout verification
- API testing
- workflow testing

without risking production systems.

## Apache + HTTPS Infrastructure

### Apache Staging Virtual Host

Created a dedicated Apache vhost:

```text
/etc/apache2/sites-available/lab.dev.benjibusco.cloud.conf
```

Configured:

```apache
ServerName lab.dev.benjibusco.cloud
DocumentRoot /var/www/dev.benjibusco.cloud/lab/app
```

Enabled the site using:

```bash
sudo a2ensite lab.dev.benjibusco.cloud.conf
```

Validated configuration:

```bash
sudo apache2ctl configtest
```

Reloaded Apache successfully.

### DNS + HTTPS Configuration

Created DNS records for:

```text
lab.dev.benjibusco.cloud
```

Verified successful DNS propagation and vhost resolution.

Configured HTTPS using Certbot:

```bash
sudo certbot --apache -d lab.dev.benjibusco.cloud
```

Successfully enabled SSL for the staging environment.

## CI/CD Deployment Architecture

### Automated Staging Deployment Workflow

Created a dedicated GitHub Actions workflow:

```text
.github/workflows/deploy-staging.yml
```

Configured automatic deployment for:

```text
push → staging
```

Deployment workflow now follows a Git-based CI/CD pipeline:

```text
1. Pull latest repository changes locally
2. Develop and test inside VS Code
3. Push commits to GitHub staging branch
4. GitHub Actions automatically deploys staging to the VPS
```

Deployment path:

```text
/var/www/dev.benjibusco.cloud/lab/app
```

Verified successful end-to-end deployment from VS Code push → GitHub Actions → VPS staging environment.

### Deployment Architecture Refinement

Refined long-term deployment architecture into:

```text
Production
https://lab.benjibusco.cloud
→ branch: main

Staging
https://lab.dev.benjibusco.cloud
→ branch: staging
```

Established a safer production workflow:

```text
1. Develop on staging
2. Test safely
3. Validate deployments
4. Merge staging → main
5. Deploy production
```

This significantly reduced production deployment risk while improving:

- rollback safety
- feature validation
- auth rollout safety
- infrastructure testing
- deployment confidence

## Observability Architecture

### Observability + API Security Review

Reviewed the current public status API exposure.

Identified data categories that should eventually become protected:

- deployment branch names
- commit hashes
- deployment metadata
- infrastructure resource metrics
- future diagnostics and logging

Planned future split between:

```text
/status/public
/status/private
```

with authenticated observability routes for internal infrastructure tooling.

## Infrastructure Overview

### Current Ecosystem Architecture

```text
Production Environment
lab.benjibusco.cloud

Production API
api.benjibusco.cloud

Staging Environment
lab.dev.benjibusco.cloud

Other Staging Sites
benkelly.dev.benjibusco.cloud
kralbenji.dev.benjibusco.cloud
```

This marked a major transition toward a more production-style multi-environment infrastructure workflow for the Benjibusco Lab ecosystem.

## Future Plans

### API Staging Environment

Future architecture:

```text
api main
→ api.benjibusco.cloud

api staging
→ api.dev.benjibusco.cloud
```

Purpose:

- pre-production auth testing
- safer deployment validation
- frontend and API staging integration
- scalable infrastructure workflows

### Public vs Private API Status Separation

Future architecture:

```text
/status/public
/status/private
```

Goal:

- preserve public observability
- protect internal deployment and infrastructure metadata
- prepare for authenticated observability tooling

### Protected Route Expansion

Potential future protected environments:

```text
/tools/
/dashboard/
/sandbox/freelance/
/client-portals/
/admin/
```

Future expansion goals:

- route-level auth scaling
- role-based permissions
- protected dashboard widgets
- authenticated observability tooling
- admin systems
- client systems

### Git Submodule Workflow Review

Areas to revisit later:

- detached HEAD handling
- submodule update flows
- staging branch integration
- deployment synchronization
- long-term devlog workflow simplification
