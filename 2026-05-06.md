# Dev Log — May 6, 2026

### Focus for Today

Improve VPS security, SSH configuration, deployment reliability, and long-term Linux server administration workflows.

## VPS Infrastructure + Security

### SSH Hardening + Port Migration

Migrated SSH access from port 22 to a custom SSH port.

Updated `/etc/ssh/sshd_config`, validated configuration using `sshd -t`, safely restarted the SSH service, and verified login access from a second terminal session before disconnecting the original session.

Diagnosed an SSH socket activation issue where `ssh.socket` continued binding to port 22, disabled `ssh.socket`, and transitioned fully to standard `ssh.service` management.

Verified the SSH daemon was listening correctly on the custom port using `ss`.

### UFW Firewall Cleanup

Added the new custom SSH port rule to UFW before migration.

Verified firewall configuration using:

- `ufw status`
- `ufw status numbered`

Removed legacy:

- `OpenSSH`
- `OpenSSH (v6)`

Retained Apache Full rules for ports 80 and 443.

## Backend Infrastructure

### Node.js Installation

Installed Node.js LTS using the official NodeSource repository and installed npm.

Learned the purpose of:

- `curl`
- NodeSource repositories
- LTS software releases

Verified installation successfully and created a `test.js` file to confirm runtime functionality.

## Deployment Infrastructure

### GitHub Actions Deployment Fixes

Diagnosed GitHub Actions deployment failures caused by workflows still targeting port 22.

Added a new repository secret for the custom SSH port and updated deployment workflow YAML files to use the new secret.

Verified successful automated deployments for:

- `benkelly.design`
- `kralbenji.com`

Confirmed deployment pipelines functioned correctly after SSH migration.
