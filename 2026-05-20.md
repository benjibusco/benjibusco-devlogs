# Dev Log — May 20, 2026

## Focus for Today

Transitioned the sandbox `todo-app` from a DOM-manipulation practice project into a small state-driven frontend application with persistent localStorage architecture.

Focused on:

- application state management
- render-driven UI architecture
- localStorage persistence
- frontend rendering patterns
- scalable frontend structure
- Git submodule workflow documentation

---

## Todo App — State Architecture Refactor

### Implemented Centralized Application State

Refactored the application away from direct DOM-first rendering toward centralized JavaScript application state.

Implemented:

```js
let todos = [];
```

Introduced structured todo objects:

```js
{
  id: Date.now(),
  text: trimmedValue,
  completed: false,
}
```

Transitioned architecture from:

```text
DOM as source of truth
```

to:

```text
JavaScript state as source of truth
```

### Strengthened Understanding Of

- state-driven frontend architecture
- object-based application state
- render synchronization
- scalable frontend data flow
- source-of-truth architecture

---

## Render-Driven UI System

### Implemented Centralized Rendering

Created reusable rendering architecture using:

```js
function renderTodos()
```

Rendering flow:

```text
todos array
↓
renderTodos()
↓
DOM output
```

Refactored:

- todo item rendering
- checkbox rendering
- delete button rendering
- completed-state rendering
- empty-state rendering

Removed direct DOM ownership from:

- submit handler
- checkbox handler
- delete handler

### Strengthened Understanding Of

- render lifecycle patterns
- centralized UI generation
- render synchronization
- frontend rendering architecture

---

## Persistent Storage Implementation

### Implemented localStorage Persistence

Implemented:

```js
localStorage.setItem();
localStorage.getItem();
JSON.stringify();
JSON.parse();
```

Created reusable persistence helper:

```js
function saveTodos()
```

Added:

- save on create
- save on completed toggle
- save on delete
- restore todos on page load
- restore completed state on page load

Application startup flow:

```text
load localStorage
↓
parse saved todos
↓
renderTodos()
```

### Strengthened Understanding Of

- browser persistence systems
- serialization/deserialization
- application lifecycle flow
- persistent state restoration

---

## Checkbox & Delete Refactors

### Refactored Completed-State Logic

Previous architecture:

```text
toggle DOM classes directly
```

New architecture:

```text
update state
↓
saveTodos()
↓
renderTodos()
```

Implemented persistent completed-state restoration after refresh.

### Refactored Delete Behavior

Refactored delete functionality into state-based deletion using:

```js
todos = todos.filter(...)
```

Architecture:

```text
remove from state
↓
saveTodos()
↓
renderTodos()
```

### Strengthened Understanding Of

- state-based rendering
- array filtering
- immutable-style updates
- UI synchronization patterns
- frontend framework-style thinking

---

## Git Submodule Workflow Documentation

### Rewrote

- `git-submodule-workflow.md`
- clarified submodule update scenarios
- separated workflows based on `git status` results
- documented stash/recovery workflows
- documented detached HEAD behavior
- documented submodule troubleshooting scenarios
- clarified `main` vs `master` branch usage between `lab` and `ben-dev-log`

### Strengthened Understanding Of

- Git submodule pointer architecture
- detached HEAD behavior in submodules
- parent repo vs submodule commit flow
- troubleshooting submodule state mismatches
- safe stash/recovery workflows
- submodule synchronization logic

---

## Workflow & Lab Architecture Planning

### Frontend Workflow Improvements

Used isolated VS Code workspace focused exclusively on:

```text
sandbox/experiments/todo-app
```

Improved:

- navigation clarity
- focused frontend iteration
- experiment-level workflow organization
- cleaner Git staging/commits

### Long-Term Lab Planning

Planned future PostgreSQL-backed authentication architecture for the Lab.

Initial direction:

```text
Public Access
→ projects
→ sandbox experiments
→ public docs

Guest Accounts
→ limited project visibility
→ temporary shared access

Admin Dashboard
→ private developer tools
→ deployment systems
→ infrastructure controls
```

Discussed future systems:

- users
- sessions
- roles
- permissions
- protected developer tooling
- temporary guest access
